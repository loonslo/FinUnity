# 资产配置规划 SOP — 产品需求文档（PRD v1.3）

> 基于：资产配置规划标准作业流程（SOP v2026-06-06）
> 修订：v1.3（2026-06-06）新增 Skill 稳定性架构与工作流取舍，修复 4 项内容问题
> 目标：Skill 先行 → Web App 后做
> 计算深度：引导 + 模板（用户手动填数，不含实时行情）
> 不构成投资建议

---

## 变更说明（v1.2 → v1.3）

> 本版核心：解决 Skill 阶段最大的风险——**对话式 LLM 的不稳定性**。原 PRD 通篇定义"算什么"，却未定义"由谁算、状态存哪、怎么校验"，导致 Skill 的可靠性完全依赖模型自觉。本版补上稳定性架构，并界定 Phase 1 与工作流的取舍。

| # | 问题 | 修复内容 |
|---|---|---|
| G | **Skill 状态/计算/校验机制缺失（最高风险）**：10 步长对话存在状态漂移、心算出错、跳步与回退无法强制、禁荐股靠自检等不稳定性，PRD 未规定任何稳定化手段 | 新增 **2.8 节 Skill 稳定性架构**（状态外置 / 计算外置 / 校验节点三原则）+ **3.0 节 Phase 1 与工作流取舍说明** |
| H | **步骤 1 自检对象不存在**：规则为"逐项加总 == 各平台总额"，但模板只收逐项行，"各平台总额"无独立输入 | 修订步骤 1：新增"平台申报总额"输入列，逐项加总与之对账 |
| I | **步骤 5 上限与步骤 3 目标可能冲突**：cap_pct < 2.6 桶目标时，截断/回拨/报错规则未定义 | 新增 **2.9 节 上限与目标冲突处理**：定义截断 + 余额回拨顺序 + 校验 |
| J | **悬空需求**：步骤 7"长期期望年化"无数据源/算法；"置信度"标注无判定标准 | 长期期望年化改为可选、给出参考算法与免责；新增 **2.10 置信度判定标准** |

> v1.0→v1.1（14 项）、v1.1→v1.2（6 项）修复记录见附录 A。

---

## 一、目标

把 10 步 SOP 做成两层工具：

| 层 | 形态 | 核心价值 |
|---|---|---|
| Phase 1 | Claude Skill | 对话式引导，逐步收集输入，生成个人化方案文档 |
| Phase 2 | Web App | 表单 + 自动计算 + 可视化，输出可打印报告 |

> **两期边界原则**：Phase 1 的差异化价值是"对话式引导"，不是"自动计算"。计算与可视化是 Phase 2 的价值。因此 Phase 1 不追求把流程做成刚性工作流，而是"对话外壳 + 确定性脊椎"（见 2.8、3.0）。

---

## 二、核心数据定义

> 本节集中定义全文复用的常量和规则，避免各步骤分散定义造成不一致。

### 2.1 细粒度桶定义（步骤 4、6、8 共用）

| 桶 ID | 名称 | 归属层/账户 | 说明 |
|---|---|---|---|
| B1 | 现金 | 防守层 | 活期/货基，可随时取用 |
| B2 | 弹药 | 防守层 | 专用于回撤加仓，非日常周转 |
| B3 | 核心宽基权益 | 稳健层 | 宽基指数，低跟踪误差，长期持有 |
| B4 | 价值/红利/低波权益 | 稳健层 | 防御型权益，关注估值区间 |
| B5 | 成长/科技权益 | 进取层 | 高波动，必须设比例上限 |
| B6 | 境外权益通道 | 进取层 | 币种/市场分散工具 |
| B7 | 训练仓/主题卫星 | 进取层 | 小比例，不向下摊平，写明退出条件 |

> **注**：用户可根据实际持仓增减子桶（如将 B3 拆分为 A股宽基/港股宽基），但默认框架为以上 7 桶。

> **层映射**：防守层 = {B1, B2}；稳健层 = {B3, B4}；进取层 = {B5, B6, B7}。步骤 3 输出的"防守% / 稳健% / 进取%"即按此映射拆到桶（拆分规则见 2.6）。

### 2.2 步骤 3 经验法调整系数（量化）

| 条件 | 调整幅度 |
|---|---|
| 基线 | 65% |
| 收入稳定且有持续结余 | +5% |
| 现育儿 | −5% |
| 计划 1 年内育儿 | −5% |
| 收入不稳定（自雇/合同工/待业） | −5% |
| 负债较重（月还款 > 收入 30%） | −5% |

> 取反推法和经验法结果中**更保守（更低）者**为最终权益比。
> 进取层占比 = 最终权益% 中分配给进取层的部分；防守层 = 100% − 稳健层% − 进取层%。三层的具体落位见 2.6。

### 2.3 步骤 7 历史情景参考跌幅

> **免责**：以下为参考区间值，非精确回测数据，不含税费/汇率/择时，过去不代表未来。用户可自行修改。

| 情景 | B3 核心宽基 | B4 价值低波 | B5 成长科技 | B6 境外权益 | B7 训练仓（默认=B5） | B1 现金/B2 弹药 |
|---|---|---|---|---|---|---|
| 2022 科技回调 | −30% | −15% | −55% | −35% | −55% | 0% |
| 2020 新冠闪崩 | −35% | −25% | −40% | −35% | −40% | 0% |
| 2008 系统性危机 | −55% | −40% | −60% | −50% | −60% | 0% |
| 2000 科技泡沫 | −45% | −20% | −80% | −45% | −80% | 0% |

> B7 训练仓默认跌幅 = 同情景 B5 值（已预填于上表及数据模型），用户可改为更深值。

### 2.4 步骤 6 加仓阶梯默认参数

仅适用于 B3 核心宽基权益（低杠杆、低费率、可长期持有）：

| 回撤深度 | 资金来源 | 投入比例（相对 1 份） |
|---|---|---|
| −5% ～ −10% | 提高定投 | 1 |
| −10% ～ −20% | 定投 + 弹药 | 1.5 |
| −20% ～ −30% | 弹药为主 | 2 |
| > −30% | 弹药 + 备用通道 | 2.6 |

> 各档之和 = 弹药（B2）总额度。前提：B1 现金 ≥ 底线后才动手。

### 2.5 步骤 8 每月瀑布决策树

```
每月结余到账后，按以下顺序判断：

IF B1 现金 < B1 现金底线（步骤3，见2.7）:
  → 100% 补 B1，停一切权益新增
  → 结束

ELSE IF 步骤6中任意回撤信号已触发:
  → 按触发档位执行加仓阶梯（定投 + B2 弹药按比例投入 B3）
  → 结束

ELSE IF 防守层（B1+B2）< 防守层目标金额（2.6 推算）:
  → 100% 补 B2 弹药至目标
  → 结束

ELSE:
  → 计算各桶缺口率 = (目标 − 现有) / 目标   （目标=0 的桶按步骤4规则跳过）
  → 投入缺口率最大的桶
  → 若多桶缺口率相近（差值 < 5 个百分点），按 B3→B4→B5→B6 优先级排序

大额到账（理财到期等）：
  先确认 B1 ≥ 底线 → 再确认 B2 满 → 余额进"权益建设池"
  按 12–18 个月分批投入，权重向缺口率最大的桶倾斜
  高位（估值贵）则暂放货基按月慢投，不追高
```

### 2.6 三层比例 → 7 桶目标金额拆分规则

> 解决：步骤 3 只产出三层百分比，而步骤 4/6/8 需要 7 桶各自的目标金额。本节定义默认拆分，用户可在步骤 5 调整每桶上限时一并覆盖（冲突处理见 2.9）。

**第一步：三层金额**
```
防守层金额 = 防守% × 策略盘
稳健层金额 = 稳健% × 策略盘
进取层金额 = 进取% × 策略盘
```

**第二步：层内拆分到桶（默认比例，用户可调）**

| 层 | 桶 | 默认层内占比 | 约束 |
|---|---|---|---|
| 防守层 | B1 现金 | B1 目标 = B1 现金底线（2.7 定义），优先满足 | 不参与百分比分配，取绝对额 |
| 防守层 | B2 弹药 | = 防守层金额 − B1 目标 | 若结果 < 0，提示防守层比例不足以覆盖现金底线，回步骤 3 |
| 稳健层 | B3 核心宽基 | 60% | 用户可调，B3+B4 = 100% |
| 稳健层 | B4 价值低波 | 40% | 同上 |
| 进取层 | B5 成长科技 | 50% | **必须设比例上限**（步骤 5），默认 ≤ 进取层 60% |
| 进取层 | B6 境外权益 | 40% | 受境外额度约束（步骤 6） |
| 进取层 | B7 训练仓 | 10% | 硬上限 = 策略盘 5%，超出部分回拨 B5/B6 |

**校验**：7 桶目标金额之和必须等于策略盘（允许 ±1 元舍入）。Skill/App 在步骤 3 输出后即展示该拆分表，并校验合计。

### 2.7 生存层与 B1 现金底线的关系（消除双算）

| 资金池 | 所在 | 口径 | 用途 | 是否计入策略盘 |
|---|---|---|---|---|
| **生存层** | 策略盘**之外** | 月刚性开支 × 保守月数（步骤 2） | 应急生存、不参与任何配置与加仓 | **否**（已从总资产扣除） |
| **B1 现金底线** | 策略盘**之内**（防守层） | 默认 = 策略盘 × 5%，或用户指定绝对额；**不再用"月开支×月数"口径** | 配置周转 + 加仓动手前的流动性缓冲 | **是** |

规则：
- 两池**物理与口径均独立**，不重叠、不相互替代；展示时分别列示。
- 加仓阶梯（2.4）动用的是 B2 弹药，前提是 **B1 ≥ B1 现金底线**；生存层任何时候都不得动用。
- 若用户坚持用"月数"定义 B1 底线，Skill 必须显式提示重叠风险。

### 2.8 Skill 稳定性架构【v1.3 新增 · 核心】

> **设计原则：对话只做外壳，脊椎做成确定性。** Skill 的差异化价值是对话引导；但凡涉及"记住数字、做算术、守规矩"的部分，一律不依赖模型自觉，改由外置机制保证。以下三条原则在单文件 SKILL.md 内即可实现（用文件工具 + 内嵌脚本）。

#### 原则一：状态外置（消除漂移）

- Skill 维护一个 **`plan_state.json`**，结构 = 4.4 数据模型。
- **每步结束写入，每步开始先读取**：模型在进入任一步骤前，先读 `plan_state.json` 恢复上下文，而非依赖对话记忆。
- 所有跨步引用（步骤 7 用步骤 4 桶权重、步骤 8 用步骤 3 目标）一律从 state 读取，禁止"凭印象"复述数字。
- **回退即改状态**：3.8 的"清除步骤 N 数据"落地为删除 `plan_state.json` 中对应字段并标记 `dirty`，下游步骤重算——3.8 从此可执行而非口头承诺。

#### 原则二：计算外置（消除算术错误）

- 下列计算**全部交给内嵌脚本**（Python/JS），模型只负责收集输入、调用、解释结果，不做心算：
  - 步骤 1 折汇与对账差额
  - 步骤 2 生存层 / 策略盘
  - 步骤 3 反推法、经验法、min、2.6 七桶拆分、合计校验
  - 步骤 4 缺口、缺口率（含除零保护）、结构病阈值判断
  - 步骤 7 Σ(桶权重 × 跌幅)、最坏浮亏、M 校验
- **副产物**：这段计算逻辑就是 Phase 2 App 的计算层，**一次编写、两期复用**，是 Phase 1 投入降 Phase 2 风险的关键。

#### 原则三：校验节点化（消除"靠自觉"）

| 校验项 | 实现方式（确定性，非模型自检） |
|---|---|
| 禁荐股 | 脚本/正则扫描步骤 5、8 输出，命中股票/基金代码或具体产品名 → 拦截并替换为类别描述 |
| 不可跳步 | 进入步骤前读 state 判断前置字段是否齐全（如 step3.M 为空 → 拒绝进入步骤 7） |
| 七桶合计 | 步骤 3 输出后脚本校验 Σ桶目标 == 策略盘（±1元），否则不放行 |
| M 校验 | 步骤 7 脚本判断 worst_loss_abs ≤ M，否则强制回步骤 3 |
| 数据标注 | 输出模板内置"截至日期 / 来源 / 置信度"占位，缺失即拦截（置信度判定见 2.10）|

> **稳定化后的残余不确定性**仅剩"措辞与引导风格"，属无害波动。所有影响结果正确性的环节均已确定化。

### 2.9 上限与目标冲突处理【v1.3 新增】

当步骤 5 用户设的桶上限 `cap_pct`（×策略盘 = cap_amount）小于 2.6 推算的桶目标时：

1. 该桶目标**截断**至 cap_amount。
2. 差额（原目标 − cap_amount）按**同层内其他未触顶桶**的默认比例**回拨**；同层无可承接桶时，回拨顺序为 进取层→稳健层→防守层 B2。
3. 回拨后重新校验"7 桶合计 == 策略盘"。
4. 若所有权益桶均触顶导致无法消化差额 → 提示"上限设置过紧，剩余资金将留存 B1/B2"，并展示留存金额。

> Skill/App 在步骤 5 保存时即执行该重算并展示调整后的七桶目标。

### 2.10 置信度判定标准【v1.3 新增】

每个财务结论标注的"置信度"按数据来源确定，规则固定可复现：

| 置信度 | 判定条件 |
|---|---|
| 高 | 用户直接提供的确定数字（账户余额、月开支）经对账无差额 |
| 中 | 由用户输入经公式推算（攻防比、七桶目标、缺口率） |
| 低 | 含参考假设或历史区间（步骤 7 情景跌幅、长期期望年化、跌幅假设） |

---

## 三、Phase 1 — Claude Skill

### 3.0 Phase 1 与工作流的取舍说明【v1.3 新增】

> 回答"是否把 Skill 改造成工作流"。

**什么是工作流改造**：把 10 步整段对话拆成一张有向图，每步是边界清晰、单独调用的小节点，节点间用状态契约传值，由控制流（而非 prompt 提示）驱动跳步与回退。

**正面影响**
- 每节点上下文短、职责单一 → 指令遵循率大幅提升，长上下文衰减消失。
- 状态成显式契约 → 漂移根除；回退 = 重算下游节点，天然支持。
- 计算/校验变代码节点 → 确定、可单测，验收标准（第六章）逐条变成 per-node eval。
- 跳步、禁荐股从软约束变真控制流。

**代价**
- **削弱 Skill 差异化**：刚性工作流让 Skill 退化为文字版向导，与 Phase 2 边界模糊，等于提前干了 App 的活。
- **平台契合度**：Cowork Skill 标准为单个 SKILL.md，无原生工作流引擎；"工作流"只能用"状态文件 + 逐步前置判断 + 脚本"模拟，靠纪律维持。
- **成本上升 / 两期重复**：过度工作流化抬高本应轻快的 Phase 1，并与 Phase 2 重复造轮子。

**决策（采纳）**：Phase 1 **不做完整工作流**，走中间路线——保留对话外壳（其存在理由），用 2.8 三原则把脊椎确定化；**完整状态机/工作流留给 Phase 2**。Phase 1 的 `plan_state.json` 结构与计算脚本 100% 顺延 Phase 2，一份投入降两期风险。

### 3.1 触发词

```
"开始资产配置"
"帮我做资产配置方案"
"走一遍资产配置 SOP"
```

### 3.2 整体流程

```
invoke skill → 读取/初始化 plan_state.json
  └─ 步骤 0：输出原则确认 → 用户确认继续 → 写 state
  └─ 步骤 1：收集全口径资产（含平台申报总额对账）→ 脚本计算 → 写 state
  └─ 步骤 2：分三层 → 脚本算策略盘 → 写 state
  └─ 步骤 3：收集 M 和约束 → 脚本双公式取 min + 2.6 七桶拆分 + 合计校验 → 写 state
  └─ 步骤 4：持仓映射到7桶 → 脚本算缺口/缺口率/结构病 → 写 state
  └─ 步骤 5：资产类别偏好 + 上限（含2.9冲突重算）→ 无荐股 → 写 state
  └─ 步骤 6：生成信号→应对表（含加仓阶梯默认参数）→ 写 state
  └─ 步骤 7：压力测试（分桶精算复核）→ 脚本 M 校验 → 写 state
  └─ 步骤 8：输出落地执行表（含每月瀑布决策树）→ 写 state
  └─ 步骤 9：输出复盘模板 + 版本说明
  └─ 汇总：由 state 渲染完整方案 .md 文件
```

> 每步进入前先读 state 校验前置条件（2.8 原则三），不满足则拒绝并说明触发点。

### 3.3 每步 Skill 行为规范

**步骤 0（原则确认）**
- 输出固定原则清单（直接展示，无需用户填写）
- 必须包含：只应对不预测 / 区分事实-推理-假设-观点 / 红线清单 / 不构成投资建议
- 用户输入 `确认` 后才进入步骤 1；不可跳过

**步骤 1（全口径资产）**
- 提供账户分类模板（银行/支付宝/券商A/券商B/海外券商/港卡/黄金/现金）
- 用户逐行填入：平台、项目名、币种、金额；并为每个平台填入**该平台申报总额**
- 脚本计算：外币 × 用户提供汇率 → 折本币合计
- **自检对账**：按平台分组的逐项加总 == 该平台申报总额，脚本展示每平台差额；差额 ≠ 0 必须先平账
- 输出格式：

```
| 平台 | 项目 | 币种 | 原币金额 | 汇率 | 折本币 |
平台申报总额：____  逐项加总：____  差额：____
截至日期：____  数据来源：用户自填
```

**步骤 2（三层分层）**
- 输入：月刚性开支、保守月数（3-6）、是否设深层储备（是/否，金额）
- 计算：生存层 = 月开支 × 月数；策略盘 = 总资产 − 生存层 − 深层储备
- 输出：三个数字 + 说明（生存层明细列表）
- 明确：生存层为策略盘之外的独立资金池（见 2.7）

**步骤 3（攻防比）**
- 输入：年龄、家庭阶段、收入稳定性、负债情况、M（最大可承受浮亏金额）、熊市跌幅假设（默认 55%，可选 45%/50%/55%）
- 脚本计算（展示过程）：
  - 反推法（**初筛**）：权益上限 = M ÷ (策略盘 × 跌幅假设)
  - 经验法：基线 65% ± 阶段调整（按 2.2）
  - 最终权益% = min(反推法, 经验法)
- **初筛定位说明**：单一跌幅初筛，步骤 7 用分桶跌幅精算复核，口径不同属正常；建议跌幅假设取偏保守值以降复核返工概率。
- 输出：防守 X% / 稳健 Y% / 进取 Z%（各自金额）+ 计算过程
- 输出：B1 现金底线金额（默认 = 策略盘 × 5%，见 2.7）
- 输出：按 2.6 拆出的 7 桶目标金额表，脚本校验合计 = 策略盘

**步骤 4（现状映射）**
- 用步骤 1 持仓逐笔归类到 7 桶（B1-B7）
- 脚本计算每桶：现有 / 目标 / 缺口 / 缺口率
- **缺口率（除零保护）**：目标>0 → (目标−现有)/目标；目标=0 且现有>0 → 显示 `—`、触发"超配"；目标=0 且现有=0 → `—`（不适用）
- 结构病检测：超配（现有 > 目标×110%，目标=0 时现有>0 即超配）/ 不足（目标>0 且现有 < 目标×90%）/ 碎片（同桶 ≥3 只同风格）/ 重复暴露（人工判断）
- 输出：缺口表（缺口率排序，`—` 置底） + 结构病清单

**步骤 5（资产类别）**
- 展示 7 桶 × 资产类别框架
- 用户为每桶选择资产类别、填上限 `cap_pct`、禁买条件
- **上限冲突重算**：按 2.9 处理 cap < 目标 的情形，展示调整后七桶目标
- 输出：每桶筛选标准卡片
- **禁荐股实现**（见 3.4，脚本拦截）

**步骤 6（信号→应对）**
- 展示 8 类预填信号框架（回撤/泡沫/再平衡/估值/境外额度/生活/限购/单一高风险权益）
- B3 加仓阶梯预填默认参数（2.4），用户可改阈值和资金比例
- 用户填入触发阈值
- 输出：完整信号→动作表

**步骤 7（压力测试 — 精算复核）**
- 展示 2.3 参考跌幅表（注明免责，紧跟呈现）
- 用户确认/修改各情景跌幅（含 B7，默认=B5）
- 脚本计算：组合峰谷 ≈ Σ(桶权重 × 跌幅)；各情景最坏浮亏 vs M
- **与步骤 3 关系**：初筛 vs 精算复核，可能出现步骤 3 通过、步骤 7 不通过（常因 B5 −80% 深于初筛），**以步骤 7 为准**
- **硬校验**：任一情景浮亏 > M → 警告 + 强制回步骤 3 调低权益%（或下调 B5/B7），返工后重跑
- 输出：情景压测表 + **长期期望年化（可选）**

> **长期期望年化（可选）**：若用户提供各桶长期年化假设，脚本算 Σ(桶权重 × 假设年化)；用户不提供则跳过此项。置信度=低，附免责"为假设推演，非预测"。

**步骤 8（落地执行）**
- 自动填本周清单（对应步骤 2 生存层隔离操作）
- 展示 2.5 每月瀑布决策树（用户数据填充）
- 用户填：月结余、大额到账（可选）
- 自动生成：每月投入分配（缺口率排序，`—` 桶不参与）
- 输出：本周清单 / 每月瀑布 / 到账分配 三张表

**步骤 9（复盘模板）**
- 输出月/季/年复盘 checklist（无需输入）
- 版本管理说明（见 3.5）

**汇总输出**
- 由 `plan_state.json` 渲染合并为完整 `.md`（降低自由生成造成的格式漂移）
- 文件名：`资产配置方案-v1.0-YYYYMMDD.md`（多用户加 owner 前缀，见 3.5）
- 保存路径：见 3.5；多方案用 `-v{N}` 区分

### 3.4 禁荐股约束实现规范

Skill prompt 必须包含以下控制机制：

**Negative instruction（明确禁止）**
```
禁止在步骤5、步骤8的输出中出现：
- 任何具体股票名称、代码（A股/港股/美股）
- 任何具体基金名称、基金代码（如 510300、QDII-xxx）
- 任何ETF、LOF、FOF、指数增强基金的具体产品名
- 任何"推荐XX"、"可以买YY"等建议性表述
```

**允许的输出形式**
```
✅ "宽基权益类（追踪沪深300或全A），筛选：规模>50亿、跟踪误差<0.5%、费率<0.15%"
✅ "候选产品由用户自行查找并在个案方案中二次确认"
❌ "可以考虑易方达沪深300ETF"
❌ "推荐510300"
```

**验证步骤（确定性）**：步骤 5、8 输出前由脚本扫描（正则匹配 6 位基金代码 / 股票代码 / 已知产品名词典），命中即拦截并替换为类别描述——不依赖模型自检（见 2.8 原则三）。

### 3.5 版本管理规范（适配用户 vault）

```
默认（单用户）：
  当前版本：wiki/topics/资产配置方案-v{N}-YYYYMMDD.md  （标记 🔄）
  历史版本：archive/资产配置方案-v{N-1}-YYYYMMDD.md    （标记 📦）
  状态文件：plan_state.json（与当前版本同目录，回退/续做依赖它）
  index 登记：wiki/index.md 中维护当前版本路径

多用户：以 owner 子目录隔离：
  wiki/topics/{owner}/资产配置方案-v{N}-YYYYMMDD.md
  archive/{owner}/资产配置方案-v{N-1}-YYYYMMDD.md
  （owner 取 meta.owner；为空时退回默认单用户路径）

文件内必须包含：版本号 / 相对上一版 diff / 生成日期

多方案管理：
  大版本 v1.0→v2.0 = 攻防比调整；小版本 v1.1→v1.2 = 阈值/产品微调
```

### 3.6 Skill 文件结构

> ⚠️ Cowork skill 系统以 **SKILL.md 单文件**为标准格式。步骤引导逻辑、模板、红线规则、稳定化脚本均内嵌或随 SKILL.md 提供。

```
skills/asset-allocation/
├── SKILL.md       # 触发词 + 10步引导 + 模板 + 红线规则 + 调用脚本说明
└── calc.py        # 2.8 计算外置 + 校验节点（步骤1-7 全部计算）
                   # （若 skill 系统仅允许单文件，则将脚本以代码块内嵌于 SKILL.md，运行时落盘执行）
```

SKILL.md 内部分节结构：
```markdown
# Asset Allocation SOP Skill
## 触发词
## 稳定性约定（读写 plan_state.json / 调用 calc.py / 校验节点）
## 步骤引导逻辑（0-9）
## 模板库
  ### T1 全口径资产表（含平台申报总额对账）
  ### T2 三层→7桶目标拆分表（2.6）
  ### T3 7桶缺口表
  ### T4 信号→应对表（含阶梯默认参数）
  ### T5 压力测试表（含情景跌幅）
  ### T6 执行表（含瀑布决策树）
  ### T7 完整方案输出格式
## 约束规则
  ### 红线
  ### 禁荐股规则
  ### 不可跳步规则
```

### 3.7 Skill 约束

| 约束 | 规则 |
|---|---|
| 红线 | 下单/调仓/连账户/改持仓记录 → 不执行，说明触发点后停止 |
| 产品建议 | 步骤5和步骤8不得输出具体股票/基金代码（脚本拦截，见3.4）|
| 数据标注 | 每个财务结论标注截至日期 + 数据来源 + 置信度（2.10）|
| 回测声明 | 步骤7必须展示免责声明，且紧跟压测结果呈现 |
| 不可跳步 | 读 state 校验前置：步骤3（M）未完成→不得进步骤7；步骤1未完成→不得进步骤4；步骤3未产出7桶目标→不得进步骤4 |
| 状态一致 | 跨步引用一律从 plan_state.json 读取，禁止凭记忆复述数字（2.8 原则一）|
| 步骤回退 | 见3.8 |

### 3.8 步骤回退处理规则

> **落地**：回退 = 删除 `plan_state.json` 中对应步骤字段并标记下游 `dirty`，确认后由脚本清除并重算。

| 回退场景 | 处理方式 |
|---|---|
| 步骤4-9 → 回退到步骤1（修改资产金额） | 步骤2-9 字段清除，从步骤2重算 |
| 步骤4-9 → 回退到步骤2（修改生存层） | 步骤3-9 字段清除 |
| 步骤4-9 → 回退到步骤3（修改M或攻防比） | 步骤4-9 字段清除（含 2.6 七桶目标）；提示"攻防比变更，后续需重填" |
| 步骤7-9 → 回退到步骤6（修改信号阈值） | 仅步骤8-9 清除；步骤7 结果保留但标"参数已变更，建议重跑" |
| 步骤9 → 回退到步骤8 | 仅步骤9 清除 |

> 回退时 Skill 明确告知将清除哪些步骤，确认后才执行。

---

## 四、Phase 2 — Web App

### 4.1 技术选型

- 单文件 HTML + React（可在 Cowork 直接运行，无需部署）
- 跨会话持久化：JSON 文件导出/导入（不使用 localStorage）
- 计算层：纯前端 JS，无后端，无外部 API —— **直接移植 Phase 1 的 calc.py 逻辑**（见 2.8）

### 4.2 页面结构

```
Header：当前步骤 / 总进度（步骤 X/10）
Sidebar：步骤导航（已完成✓ / 当前→ / 待完成○ / 锁定🔒）
Main：当前步骤的表单 + 说明
Footer：上一步 / 下一步（锁定条件未满足时禁用） / 保存草稿
```

### 4.3 各步骤 UI 规格

| 步骤 | UI 类型 | 关键计算 | 锁定条件 |
|---|---|---|---|
| 0 | 原则展示 + 全部勾选框 | 无 | 全部框勾选 |
| 1 | 动态行表格 + 汇率 + 平台申报总额 | SUM + 折汇 + 分平台对账差额 | 各平台差额 = 0 |
| 2 | 3 个数字 input + 月数 slider | 自动算策略盘（生存层独立展示） | 策略盘 > 0 |
| 3 | 5 个 input/radio + M + 跌幅假设 | 双公式取 min，按2.6算7桶目标并校验合计 | M>0 且攻防比合理 且 7桶合计=策略盘 |
| 4 | 持仓归类下拉 + 自动缺口计算 | 缺口、缺口率（除零保护）、结构病 flag | 所有持仓已归类 |
| 5 | 7桶 × 资产类别多选卡片 + 上限 input | 2.9 冲突重算，禁荐股约束 | 每桶至少选一类 |
| 6 | 8类信号预填表 + 阈值 input（B3阶梯预填）| 无计算 | 所有阈值已填 |
| 7 | 情景跌幅可编辑表 + 自动回撤计算 | Σ(桶权重×跌幅)，对比M | M校验通过 |
| 8 | 自动生成3张表（瀑布决策树可视化）+ 可编辑 | 依赖前序数据 | 月结余已填 |
| 9 | 复盘checklist展示 + 导出 | 无 | 无（直接可导出） |

### 4.4 数据模型（前端 State / plan_state.json）

> Phase 1 的 `plan_state.json` 与 Phase 2 前端 State **同构**，确保两期互通。

```javascript
{
  schema_version: "1.3",          // 草稿导入版本兼容检查
  meta: {
    version: "v1.0", date: "YYYY-MM-DD", currency_base: "CNY",
    owner: "", plan_name: ""
  },
  step0: { confirmed: false },
  step1: {
    accounts: [{ platform, item, currency, amount, rate, local_amount }],
    platform_declared: { /* platform -> 申报总额 */ },   // v1.3 新增，用于对账
    platform_reconcile: { /* platform -> 差额 */ },        // v1.3 新增
    total_local: 0,
    as_of_date: ""
  },
  step2: {
    monthly_expense: 0, survival_months: 6, deep_reserve: 0,
    survival_total: 0,            // 策略盘之外独立池（2.7）
    strategy_total: 0
  },
  step3: {
    age: null, family_stage: "", income_stable: null, heavy_debt: null,
    M: 0, bear_drop_assumption: 0.55,
    equity_pct_inferred: 0, equity_pct_experience: 0, equity_pct_final: 0,
    layer_pct: { defense: 0, steady: 0, growth: 0 },
    cash_floor: 0,                // 默认 strategy_total*0.05（2.7）
    split: {
      steady: { B3: 0.6, B4: 0.4 },
      growth: { B5: 0.5, B6: 0.4, B7: 0.1 }
    },
    buckets_target: { B1:0,B2:0,B3:0,B4:0,B5:0,B6:0,B7:0 },
    buckets_target_sum_check: 0   // 应 == strategy_total（±1元）
  },
  step4: {
    positions: [{ item, bucket_id, current_amount }],
    buckets_current: { B1:0,B2:0,B3:0,B4:0,B5:0,B6:0,B7:0 },
    buckets_gap:     { B1:0,B2:0,B3:0,B4:0,B5:0,B6:0,B7:0 },
    buckets_gap_rate:{ B1:0,B2:0,B3:0,B4:0,B5:0,B6:0,B7:0 }, // 目标=0 → null，UI 显示 "—"
    structure_issues: []
  },
  step5: {
    buckets: [{ bucket_id, asset_class, criteria, cap_pct, forbidden: [] }],
    target_after_cap: { B1:0,B2:0,B3:0,B4:0,B5:0,B6:0,B7:0 }  // v1.3 新增，2.9 重算结果
  },
  step6: {
    signals: [{ type, trigger_condition, action }],
    ladder: { tiers: [
      { drop_from:-0.05, drop_to:-0.10, source:"定投",        multiplier:1 },
      { drop_from:-0.10, drop_to:-0.20, source:"定投+弹药",    multiplier:1.5 },
      { drop_from:-0.20, drop_to:-0.30, source:"弹药为主",      multiplier:2 },
      { drop_from:-0.30, drop_to:null,  source:"弹药+备用通道", multiplier:2.6 }
    ]}
  },
  step7: {
    // B7 为默认预填值（=同情景 B5），用户可改（与 2.3 一致）
    scenarios: [
      { name:"2022科技回调", drops:{ B3:-0.30,B4:-0.15,B5:-0.55,B6:-0.35,B7:-0.55,B2:0,B1:0 } },
      { name:"2020新冠闪崩", drops:{ B3:-0.35,B4:-0.25,B5:-0.40,B6:-0.35,B7:-0.40,B2:0,B1:0 } },
      { name:"2008系统性",   drops:{ B3:-0.55,B4:-0.40,B5:-0.60,B6:-0.50,B7:-0.60,B2:0,B1:0 } },
      { name:"2000科技泡沫", drops:{ B3:-0.45,B4:-0.20,B5:-0.80,B6:-0.45,B7:-0.80,B2:0,B1:0 } }
    ],
    expected_annual_assumptions: null,  // v1.3：可选，用户提供各桶长期年化时才算
    results: [{ scenario_name, loss_pct, loss_abs }],
    worst_loss_abs: 0, m_check_pass: false
  },
  step8: {
    monthly_surplus: 0, lump_sum_incoming: 0,
    this_week: [], monthly_cascade: [], lump_sum_plan: []
  },
  step9: { version_notes: "", review_checklist: {} }
}
```

**版本兼容**：导入草稿检查 `schema_version`；旧版（≤1.2）缺 `platform_declared / target_after_cap / expected_annual_assumptions`，导入后提示在对应步骤补齐。

### 4.5 输出

- **导出 Markdown**：完整方案文档（同 Skill 输出格式，路径见 3.5）
- **导出 PDF**：浏览器 print-to-PDF（含 `@media print` 样式）
- **保存草稿**：JSON 文件下载（含 schema_version），下次导入恢复

### 4.6 红线 & 边界

同 Skill 约束（3.7），额外：
- 步骤 1 任一平台对账差额 ≠ 0 → "下一步"禁用
- 步骤 3 七桶目标合计 ≠ 策略盘 → "下一步"禁用 + 显示差额
- 步骤 5 上限冲突 → 展示 2.9 重算结果，若全桶触顶提示留存金额
- 步骤 7 M 校验不通过 → "下一步"禁用 + 红色提示具体差额
- 所有结果页底部固定显示免责声明
- 回退遵守 3.8：清除前弹确认 Dialog

---

## 五、实施路径

### Phase 1（Skill）— 预计工时

| 任务 | 内容 | 估计 |
|---|---|---|
| T0 | **稳定性脚手架**：plan_state.json 结构 + calc.py 计算/校验 + 读写约定 | **3h** |
| T1 | 写 SKILL.md（10步引导 + 模板内嵌 + 调用脚本说明 + 2.6-2.10 规则）| 7–9h |
| T2 | 禁荐股脚本拦截（正则 + 产品名词典 + 测试用例）| 1.5h |
| T3 | 走完整流程测试（真实数字，校验七桶合计、对账差额）| 1.5h |
| T4 | 边界与回退测试（M不通过/回退清状态/除零/上限冲突/防守层不足覆盖底线）| 1.5h |
| T5 | 修复 + 输出完整示例方案文档 | 1h |

**总计：15.5–17.5h，建议分 3-4 天完成**

### Phase 2（Web App）— 预计工时

| 任务 | 内容 | 估计 |
|---|---|---|
| T1 | 基础框架：步骤导航 + state 结构（复用 Phase1）+ 回退规则 | 2h |
| T2 | 步骤 0–3 表单 + 双公式 + 2.6 七桶推算与合计校验（移植 calc）| 4h |
| T3 | 步骤 4 持仓归类 + 缺口（除零保护）+ 结构病 | 3h |
| T4 | 步骤 5–6 配置表单（含 2.9 上限冲突重算、阶梯预填）| 2.5h |
| T5 | 步骤 7 压测 + M 校验拦截 | 2h |
| T6 | 步骤 8 瀑布决策树可视化 + 自动生成执行表 | 3h |
| T7 | 步骤 9 + 导出 MD/JSON/PDF | 2h |
| T8 | 整体 UI 打磨 + 边界测试 | 2h |

**总计：20.5h，建议分 1.5 周完成（计算层因复用 Phase1 已大幅前置降险）**

### 建议执行顺序

```
Week 1 前半：Skill T0-T1（脚手架 + SKILL.md，核心工作量）
Week 1 后半：Skill T2-T5（脚本拦截 + 测试 + 示例方案）
Week 2 前半：App T1-T4（框架 + 步骤0-6，复用计算层）
Week 2 后半：App T5-T8（步骤7-9 + 测试 + 导出）
```

---

## 六、验收标准

### 6.1 Skill 正常路径

- [ ] 完整走一遍 10 步，不跳步，由 state 渲染输出完整方案 `.md`
- [ ] 步骤 1：分平台对账差额 = 0 才放行
- [ ] 步骤 3：双公式取 min 可复现；按 2.6 拆 7 桶，合计 = 策略盘（±1元）
- [ ] 步骤 4：缺口率排序正确，目标=0 桶显示 "—" 置底
- [ ] 步骤 5：上限 < 目标时按 2.9 重算并展示
- [ ] 步骤 6：B3 阶梯默认参数 1:1.5:2:2.6 预填
- [ ] 步骤 7：M 不通过强制回步骤 3，以步骤 7 为准
- [ ] 步骤 8：每月瀑布按决策树自动生成
- [ ] 汇总：含截至日期/来源/置信度（按 2.10 标注）

### 6.2 Skill 稳定性路径【v1.3 新增】

- [ ] 关闭对话后重开，从 plan_state.json 恢复到上次步骤，数字一致
- [ ] 所有数值由脚本计算（抽查 3 处，与手算一致）
- [ ] 跨步引用（步骤7用步骤4权重）取自 state，非凭记忆
- [ ] 步骤 5/8 输出无任何股票/基金名称或代码（脚本拦截命中可演示）
- [ ] 缺前置字段（如无 M）时进入步骤 7 被拒绝并说明触发点

### 6.3 Skill 约束 & 回退路径

- [ ] 下单/调仓类请求被识别并拒绝，说明触发点
- [ ] 步骤 7 免责声明紧跟压测结果
- [ ] 从步骤 5 回退到步骤 3 修改 M → 提示清除步骤 4-9（含七桶目标），确认后 state 字段清除并重算
- [ ] 从步骤 8 回退到步骤 6 → 步骤 7 标"参数已变更"，步骤 9 清除

### 6.4 Skill 边界测试

- [ ] M = 0：提示 M 必须 > 0，阻止进步骤 7
- [ ] 策略盘 = 0：提示无可投资金额，停在步骤 2
- [ ] 步骤 1 汇率为空：提示填写，阻止进步骤 2
- [ ] 步骤 4 全归 B1：结构病触发"进取桶极度不足"
- [ ] 某桶目标 = 0：缺口率显示 "—" 不报错；现有>0 触发"超配"
- [ ] 防守层金额 < B1 现金底线：提示回步骤 3（2.6）
- [ ] 步骤 5 全桶触顶：提示留存金额（2.9）

### 6.5 Web App 功能

- [ ] 步骤导航：已完成可跳转，锁定不可跳入
- [ ] 步骤 1 分平台对账差额实时展示，=0 才进步骤 2
- [ ] 步骤 3 双公式实时计算；七桶合计实时校验
- [ ] 步骤 5 上限冲突实时按 2.9 重算
- [ ] 步骤 7 M 不通过"下一步"禁用并显示差额
- [ ] 回退 Dialog 确认后正确清除对应步骤数据
- [ ] 导出 JSON 含 schema_version=1.3，导入旧版显示字段缺失警告
- [ ] 导出 MD 与页面数据一致，含截至日期
- [ ] 免责声明在步骤 0 和最终导出页均出现

---

## 附录 A：历史修复记录（存档）

### A.1 v1.0 → v1.1（14 项）

| # | 问题 | 修复 |
|---|---|---|
| 1 | 步骤3公式硬编码偏激进 | 跌幅参数可选（默认55%，可选45%/50%/55%）|
| 2 | 步骤4桶粒度不足 | 三账户→7个细粒度桶 |
| 3 | 步骤7跌幅数据缺失 | 新增4情景×5资产类别参考跌幅表 |
| 4 | 步骤8瀑布逻辑未定义 | 新增完整决策树 |
| 5 | 步骤间回退状态规则缺失 | 新增回退处理规则 |
| 6 | 步骤6加仓阶梯默认参数缺失 | 补入 1:1.5:2:2.6 |
| 7 | Skill文件结构不兼容 | 改为SKILL.md单文件，模板内嵌 |
| 8 | 禁荐股无实现方案 | 新增 prompt engineering 规范 |
| 9 | 步骤9路径未适配vault | 明确适配 obsidian 目录 |
| 10 | 经验法阶段调整未量化 | 新增各阶段调整幅度 |
| 11 | JSON schema无版本字段 | 新增 schema_version |
| 12 | 验收标准缺边界测试 | 补充边界用例 |
| 13 | 多方案管理未说明 | 新增命名与管理规则 |
| 14 | Skill工时偏低 | T1 调整为 6-8h |

### A.2 v1.1 → v1.2（6 项）

| # | 问题 | 修复 |
|---|---|---|
| A | 三层比例→7桶目标拆分规则缺失 | 新增 2.6 |
| B | 生存层与B1现金底线双算风险 | 新增 2.7 |
| C | 步骤3与步骤7 M校验口径不一致 | 明确初筛-复核定位 |
| D | 缺口率除零 | 步骤4 + 6.4 除零保护 |
| E | step7 B7硬编码与"用户自填"矛盾 | 统一为默认预填=B5 |
| F | 多用户前缀与单vault路径混用 | 3.5 owner子目录隔离 |

### A.3 v1.2 → v1.3（4 项）

见篇首"变更说明"表（G/H/I/J）。

---

> 本文档不构成投资建议。所有投资决策由用户本人确认执行。
> PRD v1.3 · 2026-06-06
