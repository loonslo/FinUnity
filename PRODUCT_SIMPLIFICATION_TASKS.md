# FinUnity 产品精简改版需求与任务清单

> 文档状态：待执行  
> 建立日期：2026-08-30  
> 当前版本：v1.1（低频资产事件与月度复盘调整）  
> 最后更新：2026-08-30  
> 依据：`pic/00_splash.png` 至 `pic/50_landing_target_dialog.png` 全页面截图走查  
> 核心原则：少即是多；降低层级；帮助用户快速识别自己的资产情况。  
> 状态约定：`[ ]` 待做，`[~]` 进行中，`[x]` 已完成，`[!]` 阻塞，`[-]` 取消。

## 0. 文档定位与决策优先级

本文件用于跟踪新一轮“产品精简改版”，是本轮信息架构、页面层级和 UI 密度的主要依据。

- 与 `task.md`、`ROADMAP.md`、`productfixlist.md`、`usabilityfixlist.md` 中既有功能入口规划发生冲突时，以本文件的精简方向为准。
- 本轮优先调整入口和呈现方式，原则上不删除底层财务数据，也不破坏备份兼容。
- 被降级的功能先移动到“更多工具”，经过使用验证后再决定是否删除代码。
- 核心计算、数据库迁移、备份恢复和价格同步的正确性不因 UI 精简而降低。

---

## 1. 背景与当前问题

FinUnity 当前同时承担资产看板、持仓管理、交易记录、日常记账、资产配置、财务规划、压力测试和数据工具等职责。功能完整，但用户首次进入时需要理解过多概念，核心价值“识别自己的资产情况”被大量入口和说明文字稀释。

截图走查中主要问题：

- 底部存在 5 个一级入口，一级概念过多。
- “持仓、账户资产、合并持仓、资产详情、价格历史、交易流水”之间存在重复页面和重复信息。
- “记一笔、添加资产、手动录入、买入、导入持仓”等动作边界不清晰。
- 日常收支、报表、周期收支等记账能力占据了过高层级，偏离资产识别核心。
- 策略配置与完整规划同时存在，规划功能拆分过细。
- 页面普遍使用多层大卡片、大胶囊按钮和长说明，视觉层级偏重。
- 空数据页面仍展示大量筛选器、零值或说明，降低信息效率。
- `+9900%` 等疑似异常数据被高亮展示，却没有数据质量提醒。

---

## 2. 产品目标

### 2.1 核心用户问题

用户打开应用后，应能快速回答：

1. 我现在有多少资产？
2. 这些资产分布在哪里？
3. 是否存在需要我关注的异常或明显偏离？

### 2.2 本轮目标

- 一级导航从 5 项减少到 3 项。
- 首屏只呈现总额、结构、变化和一条重要提醒。
- 从首页到任一具体资产详情不超过 2 次点击。
- 将同类资产详情、价格和交易信息收口到一条路径。
- 将同类新增/交易入口收口为一个主动作。
- 默认界面隐藏低频高级能力，避免干扰核心识别任务。
- 将首屏可见卡片、主按钮和说明文本数量显著降低。
- 建立“系统每日更新、用户按事件记录、每周轻看、每月复盘、仅异常时行动”的产品节奏。
- 将传统日常记账重新定义为低频“资产事件”，用于解释资产变化，而不是要求用户逐笔记录消费。

### 2.3 非目标

本轮不以新增功能为目标，不新增券商直连、云同步、交易执行或投资推荐能力；不建设预算、账单、消费排行榜等完整记账体系；不重做三桶算法、价格同步和备份数据格式，除非 UI 收口暴露出必要的正确性问题。

### 2.4 产品节奏

FinUnity 必须明确区分数据更新频率、用户记录频率和用户决策频率：

| 层级 | 建议频率 | 内容 | 产品态度 |
|---|---|---|---|
| 系统更新 | 每日后台 | 证券价格、汇率、资产快照、数据质量 | 安静执行，不制造交易冲动 |
| 用户记录 | 重要事件发生时 | 买卖、入金、出金、大额收支、转账、分红、估值调整 | 低频、必要时才记录 |
| 轻量查看 | 每周可选 | 总资产变化、净流入、市场变化、异常状态 | 无显著变化时明确“无需处理” |
| 正式复盘 | 每月 | 期初/期末资产、净流入、投资收益、结构变化 | 提供解释和结论，不要求逐日复盘 |
| 调整行动 | 触发式 | 数据异常、风险越界、配置明显偏离 | 只有达到阈值才建议行动 |

每日更新表示数据保持新鲜，不代表鼓励用户每天打开应用、每天记账、每天复盘或每天进行交易。

---

## 3. 产品设计原则

所有页面改造必须同时满足以下约束：

1. **一页一个主任务**：每个页面只有一个最突出的操作或结论。
2. **先结论，后细节**：默认展示结果，成本、数量、时间等细节按需展开。
3. **不因功能存在就展示入口**：低频功能统一进入“更多工具”。
4. **最多两层主内容**：页面背景 + 一层内容容器；避免卡片内再套卡片。
5. **空状态做减法**：没有数据时只解释原因并提供一个下一步动作。
6. **异常优先于完整**：同一页面最多突出一条最重要提醒。
7. **数据可信优先**：异常收益、缺汇率、旧价格等状态必须明确，不把可疑数字当成果展示。
8. **术语统一**：面向用户统一使用“资产”，仅证券类场景使用“持仓”。
9. **更新不等于行动**：价格可以每日更新，但页面不因短期波动默认产生交易暗示。
10. **记录重要变化而非全部消费**：资产事件服务于解释资产变化，不追求流水的完整覆盖率。

---

## 4. 目标信息架构

### 4.1 一级导航

```text
总览
├─ 总资产与变化
├─ 三桶资产结构
├─ 重点提醒
└─ 最近资产/查看全部

资产
├─ 全部资产列表
├─ 按三桶筛选
├─ 按账户筛选
├─ 资产详情
├─ 资产变动
└─ 添加记录

我的
├─ 账户管理
├─ 数据导入
├─ 备份与导出
├─ 更多工具
└─ 设置与隐私
```

### 4.2 原入口迁移

| 当前一级入口/页面 | 新位置 | 处理方式 |
|---|---|---|
| 总览 | 总览 | 重做为核心资产看板 |
| 持仓 | 资产 | 更名并扩展为全部资产 |
| 流水 | 资产详情 / 资产→资产变动 | 改为低频资产事件与交易记录，不再作为一级入口 |
| 配置 | 总览→结构/提醒 | 不再作为一级入口 |
| 账户 | 我的 | 与数据工具合并 |
| 策略配置 | 总览→资产结构 | 与目标配置收口 |
| 完整规划 | 我的→更多工具 | 降级为高级工具 |
| 财务报表 | 月度复盘 / 我的→更多工具 | 改为资产变化解释，弱化消费分类 |
| 周期收支 | 我的→更多工具 | 仅保留工资、房贷、保费等显著事件 |
| 数据对账 | 我的→数据与安全 | 保留低频入口 |
| 大额支出模拟 | 我的→更多工具 | 降级 |
| 压力测试 | 我的→更多工具 | 降级 |
| 月度复盘 | 总览周期入口 / 我的→更多工具 | 每月主要复盘场景，不要求每日进入 |
| 落点跟踪 | 更多工具 | 降级 |

---

## 5. 核心页面需求

## 5.1 总览

### 页面必须回答

- 当前总资产/净资产是多少。
- 相比成本或上一有效快照变化多少。
- 防守、稳健、进攻各占多少。
- 当前最值得关注的问题是什么。

### 默认内容顺序

1. 顶部标题与金额显隐开关。
2. 核心资产区：总资产、本月变化；“今日变化”仅作为低强调辅助信息。
3. 三桶结构：单条堆叠比例条 + 三个比例。
4. 重点提醒：最多一条；无异常时不占位。
5. 资产摘要：最多展示前 3 项，提供“查看全部”。
6. 到达复盘周期时提供一次低打扰的“查看月度复盘”入口。

### 不在首页常驻

- 账户数量、记录数量、同步规则说明。
- 多张规划入口卡。
- 多条同时出现的配置建议。
- 完整交易流水和价格历史。
- 空数据图表或连续零值。
- 今日涨跌排行榜、默认买卖建议和强调短期波动的视觉刺激。

### 验收标准

- [ ] 正常数据下，首屏不滚动即可看到总额、变化、三桶结构和重点提醒。
- [ ] 首屏核心卡片不超过 2 个。
- [ ] 重点提醒最多展示 1 条，并可进入对应处理页面。
- [ ] 无提醒时不展示空提醒卡。
- [ ] 缺汇率、价格过期或疑似成本异常时，总额附近显示数据质量状态。
- [ ] 点击三桶结构可查看桶内资产，但不在首页展开完整列表。
- [ ] 日常状态下不出现“今天应该交易什么”的暗示；没有显著变化时允许明确展示“无需处理”。
- [ ] 月度复盘入口按周期出现，可关闭，本周期关闭后不重复打扰。

## 5.2 资产列表

### 页面结构

- 顶部摘要：资产总额 + 资产项数。
- 主列表：名称、类型/账户、市值、变化。
- 筛选入口：默认只显示一个“筛选/排序”按钮；启用后再展示条件。
- 主动作：“添加记录”。

### 交互规则

- 有 1 项资产时不常驻显示完整分类筛选栏。
- 三桶和账户筛选放在同一筛选面板，不分别占据多行。
- 列表默认按市值降序。
- 每项使用紧凑列表行或低层级容器，不使用大面积独立卡片。

### 验收标准

- [ ] “持仓”用户文案统一改为“资产”，证券明细语境除外。
- [ ] 单个列表项在常规字体下高度不超过约 88dp。
- [ ] 单项资产场景不显示无意义的多项分类筛选。
- [ ] 用户最多 2 次点击可从总览进入任一资产详情。
- [ ] 空状态只包含一句解释和“添加记录”操作。

## 5.3 资产详情

### 页面收口

将当前概览、流水、价格、独立交易流水、独立价格历史和合并持仓详情收口为一个资产详情主页面。

### 默认展示

1. 资产名称、代码、总市值、盈亏。
2. 数量、成本、最新价、币种、所属账户。
3. 最近价格变化摘要；没有足够历史时隐藏图表。
4. 最近 3 笔交易；提供“查看全部”。
5. 页面底部主操作：“记录交易”。

### 账户来源

- 单账户资产直接展示所属账户。
- 跨账户合并资产展示“分布于 N 个账户”，点击后展开来源。
- 买入/卖出前必须选择具体来源账户，但来源选择不应独立成为长期停留页面。

### 验收标准

- [ ] 用户无需在“概览/流水/价格”三个常驻 Tab 间切换即可看到核心信息。
- [ ] 价格和交易完整列表使用“查看全部”按需进入。
- [ ] 非交易型资产不展示无意义的收益率和买卖按钮。
- [ ] 买入、卖出均从同一个“记录交易”入口进入。
- [ ] 页面顶部不重复显示两次资产名称和总市值。

## 5.4 添加记录与交易

### 统一主入口

全局统一使用“添加记录”，进入后提供：

1. 截图导入。
2. 手动添加资产。
3. 记录交易。
4. 记录资金变动。

### 记录交易

- 买入/卖出共用同一页面和字段结构。
- 首屏必填字段仅包括资产、账户、数量、成交价。
- 成交金额自动计算。
- 费用、日期、备注折叠在“更多选项”。
- 默认日期为今天，不常驻占用一行，用户需要时再改。

### 记录资金变动

- 仅记录能够解释资产变化的重要事件：收入、大额支出、入金、出金、转账、分红/利息和估值调整。
- 不要求逐笔记录餐饮、交通、购物等日常小额消费。
- 支持在周末或月末录入一笔汇总，例如“8 月日常支出 ¥6,200”。
- 默认字段仅为类型、金额和账户；分类、日期、备注按需展开。
- 买入/卖出继续归入“记录交易”，不与普通资金变动混为一类。

### 验收标准

- [ ] 不再同时出现“记一笔、添加资产、手动录入、买入资产”等含义接近的一级动作。
- [ ] 买入和卖出复用同一表单组件。
- [ ] 必填项完成前按钮禁用且文字对比度符合可读性要求。
- [ ] 从资产详情发起交易时自动带入资产和来源账户。
- [ ] 费用、日期、备注默认折叠，展开后仍可完整保存。
- [ ] 用户可以用一笔周/月汇总记录日常支出，不需要补录每日明细。
- [ ] 资金变动保存后可被月度复盘用于解释总资产变化。

## 5.5 我的

### 默认分组

**账户**

- 账户列表。
- 添加账户。

**数据与安全**

- 导入数据。
- 备份与恢复。
- 导出数据。
- 数据对账。

**设置**

- 本位币。
- 提醒。
- 隐私说明。

**更多工具**

- 目标配置。
- 月度复盘。
- 资产历史。
- 财务报表。
- 周期收支。
- 落点跟踪。
- 大额支出模拟。
- 压力测试。

资产变动的快捷入口同时保留在“资产”页；它不是隐藏工具，也不是底部一级导航。

### 验收标准

- [ ] 账户卡只展示名称、类型、资产额和同步/数据状态。
- [ ] “数据工具”不再以 4～6 个同级大按钮平铺。
- [ ] 低频工具使用普通设置列表行，不使用多个全宽胶囊按钮。
- [ ] 合并规则、隐私长说明等不在账户首页常驻。
- [ ] 删除账户等危险操作只出现在账户编辑页底部，并有二次确认。

---

## 6. 功能降级与范围控制

## 6.1 低频资产事件

FinUnity 不建设传统的高频日常记账习惯。“收支/流水”能力保留，但重新定义为**低频资产事件记录器**，用于解释周度或月度资产为什么发生变化。

核心解释公式：

```text
期初资产 + 净流入 + 投资收益 + 估值调整 = 期末资产
```

### 记录范围

- 投资交易：买入、卖出、分红、费用。
- 资金进出：入金、出金、工资或其他重要收入。
- 重要支出：房租、房贷、保费、购车、装修、教育、医疗等大额支出。
- 账户转移：内部转账不改变总资产，但用于解释账户余额变化。
- 估值调整：房产、车辆、保单等非每日行情资产的估值修正。
- 日常小额消费：默认不逐笔记录，可按周或按月录入一笔汇总。

### 产品处理

- “流水”一级入口移除，改名为“资产变动”。
- 投资交易保留在资产详情及资产变动记录中。
- 资金变动入口位于“资产→添加记录”，而不是底部导航。
- 餐饮、交通、购物等细分类不在主流程中展示。
- 周期规则仅鼓励设置工资、房贷、房租、保费等显著事项。
- 不用连续记账天数、打卡、遗漏提醒等机制刺激高频记录。
- 用户即使完全不记录普通消费，也能使用资产总览、行情更新和三桶识别能力。

### 验收标准

- [ ] 页面统一使用“资产变动/资金变动”，不把产品定位描述为“每日记账”。
- [ ] 用户可按周或按月录入一笔汇总支出。
- [ ] 总览和资产页不常驻展示餐饮、交通等小额消费分类。
- [ ] 投资交易、资金流入流出和账户内部转账在数据含义上明确区分。
- [ ] 内部转账不影响净流入和总资产变化。
- [ ] 资产事件可以被月度复盘聚合并解释资产变化。
- [ ] 没有资产事件数据时，复盘明确标注“变动解释可能不完整”，但不阻止查看。

## 6.2 周度轻看与月度复盘

### 周度轻看

周度查看是可选行为，不要求用户完成清单：

- 本周总资产变化。
- 净流入与市场变化的粗略拆分。
- 数据是否存在异常或过期。
- 没有显著变化时显示“本周资产结构稳定，无需处理”。

### 月度复盘

月度复盘是资产事件的主要消费场景，默认回答：

1. 期初和期末资产分别是多少。
2. 本月净流入是多少。
3. 本月投资收益/市场变化是多少。
4. 三桶结构发生了什么变化。
5. 是否存在超过阈值、需要处理的偏离。

默认结论示例：

> 本月总资产增加 ¥12,000，其中净流入 ¥8,000，投资收益 ¥4,000。三桶结构稳定，无需调整。

### 交互规则

- 复盘以结论为主，不先展示流水列表和大量复盘清单。
- 月度复盘提醒默认低打扰，由用户主动开启；同一周期提醒一次即可。
- 数据不足时展示已有事实和缺失原因，不生成伪精确归因。
- 调整建议只有在偏离超过阈值时出现，不因每日价格波动频繁变化。

### 验收标准

- [ ] 月度复盘能够拆分净流入、投资收益和估值调整；无法拆分时明确说明。
- [ ] 月度复盘首屏先显示一句结论，再显示明细。
- [ ] 无显著偏离时明确显示“无需调整”，不强行生成建议。
- [ ] 周度查看和月度复盘均不默认提供具体买卖标的建议。
- [ ] 每日价格更新不会触发每日复盘通知。

## 6.3 规划能力

核心页面只保留：

- 当前三桶比例。
- 目标三桶比例。
- 偏离程度。
- 一条调整建议。

其余风险体验、回撤阶梯、落点、复盘、历史、大额支出模拟和压力测试统一归入“更多工具”。

### 验收标准

- [ ] “策略配置”和“目标配置”合并为一个目标配置入口。
- [ ] 总览只在偏离超过阈值时展示调整提醒。
- [ ] 同一时刻不同时展示风险过高、单只持仓过高和三桶偏离等多张提醒卡；按优先级取一条。
- [ ] 高级规划能力仍可达，但不进入底部导航。

---

## 7. 视觉与内容规范

## 7.1 页面密度

- 页面左右边距建议统一为 20～24dp。
- 卡片内部垂直间距相对当前版本降低约 20%～30%。
- 常规页面顶部标题区不使用过大的空白。
- 底部主按钮高度统一为 52～56dp。
- 同一页面最多一个高强调主按钮。
- 页面常规卡片圆角统一为 12～16dp。

## 7.2 卡片与层级

- 每页最多一个核心信息卡；其他信息优先使用普通区块或分割线。
- 禁止卡片内继续使用同等级大卡片。
- 列表项默认不使用厚重边框和大面积背景。
- 筛选器、说明文字、空状态不额外套多层容器。

## 7.3 颜色

- 品牌主色用于主操作和当前选中状态。
- 三桶蓝/黄/橙仅用于资产结构语义。
- 红绿只用于涨跌、收支或告警，不同时承担主按钮语义。
- 买入/卖出按钮建议使用中性主色 + 文案区分，避免与涨跌颜色混用。
- 暗色界面减少纯白大面积按钮，主按钮使用品牌色。

## 7.4 文案

- 说明文案默认不超过 2 行。
- 长说明进入“了解更多”或帮助弹窗。
- 删除实现细节式文案，如数据库、自动归集算法和持仓合并规则的常驻说明。
- 空状态格式统一为：状态标题 + 一句原因/价值 + 一个动作。
- 避免“进入完整规划”“保存并加入汇总”等系统式表达，改成用户任务语言。

### 验收标准

- [ ] 随机抽查 10 个核心页面，没有卡片套卡片的同等级视觉结构。
- [ ] 每页高强调按钮不超过 1 个；交易买卖并列场景除外。
- [ ] 所有禁用按钮文字可读，不出现灰底灰字或浅底白字。
- [ ] 说明文字超过 2 行时必须说明其保留理由或改为按需展开。
- [ ] 空状态不展示无意义的筛选控件、零值列表或空图表。

---

## 8. 数据质量与异常表达

### 规则

- 累计收益率超出合理阈值时，不直接作为普通收益高亮；展示“收益率异常，请检查成本价”。
- 缺少汇率时明确说明哪些资产未计入总额。
- 行情过期或使用缓存时，在金额附近显示状态，不把旧价格标记为已同步。
- 只有一个历史价格点时不展示趋势暗示，显示“暂无足够历史数据”。
- 连续 12 个月均为零时，财务报表显示空状态，不渲染 12 张零值卡。

### 验收标准

- [ ] `+9900%` 示例数据触发成本异常提示。
- [ ] 缺汇率、旧价格和识别待确认状态在总览或资产详情可被发现。
- [ ] 异常状态具有明确处理入口，例如编辑成本、刷新价格或确认代码。
- [ ] 数据质量提示优先级高于一般配置建议。

---

## 9. 分阶段执行任务

## Task S0 `[ ]`：冻结改版基线

**优先级**：P0  
**目标**：保留当前截图、导航和核心流程作为对照，避免在大量未提交改动上丢失行为。

- [ ] 将 `pic/` 截图索引与当前页面/导航入口对应。
- [ ] 记录当前 5 个底部入口和全部二级入口。
- [ ] 标记底层能力必须保留、仅入口降级和候选删除三类状态。
- [ ] 对总览、资产录入、交易、账户、备份建立改版前冒烟路径。
- [ ] 确认本轮不覆盖用户现有未提交修改。

**完成标准**：形成可核对的改版前页面映射与核心流程清单。

## Task S1 `[ ]`：将一级导航收口为 3 项

**优先级**：P0  
**依赖**：S0  
**主要范围**：`MainActivity.kt`、底部导航组件、顶层 Screen 定义。

- [ ] 新一级导航定义为“总览 / 资产 / 我的”。
- [ ] “流水、配置”移出底部导航。
- [ ] “持仓”更名为“资产”。
- [ ] 账户和数据工具并入“我的”。
- [ ] 保留所有被迁移功能的稳定返回路径。
- [ ] 修正系统返回和 Tab 切换的栈行为。

**完成标准**：冷启动后只显示 3 个底部入口；原有核心能力仍可通过新路径访问。

## Task S2 `[ ]`：重做总览

**优先级**：P0  
**依赖**：S1  
**主要范围**：当前总览/主屏相关 Compose 页面、`MainViewModel.kt` 的首页展示模型。

- [ ] 建立专用 Overview UI State，只输出首页需要的数据。
- [ ] 实现总资产与变化核心区。
- [ ] 实现紧凑三桶比例条。
- [ ] 建立提醒优先级：数据质量 > 风险上限 > 三桶偏离 > 其他。
- [ ] 首页最多展示一条提醒。
- [ ] 展示前 3 项资产及“查看全部”。
- [ ] 首页核心变化周期默认使用“本月”，今日涨跌降为辅助信息。
- [ ] 增加低打扰的月度复盘入口及“本周期不再提示”状态。
- [ ] 无显著变化时允许展示“资产结构稳定，无需处理”。
- [ ] 补齐加载、空数据、部分数据不可用状态。

**完成标准**：首屏不滚动即可回答“有多少、在哪里、要注意什么”。

## Task S3 `[ ]`：统一资产列表与详情

**优先级**：P0  
**依赖**：S1、S2  
**主要范围**：资产列表、`AssetDetailScreen.kt`、`MergedHoldingDetailScreen.kt`、价格/交易历史页面。

- [ ] 建立一个统一资产列表。
- [ ] 合并三桶筛选、账户筛选和排序入口。
- [ ] 资产详情去除重复头部数据。
- [ ] 最近交易和最近价格变化内嵌为摘要。
- [ ] 完整交易/价格历史作为“查看全部”二级页。
- [ ] 合并持仓来源在同一详情页按需展开。
- [ ] 非交易资产隐藏买卖和无意义收益字段。

**完成标准**：任一资产只有一个主详情入口；用户不再面对多种近似详情页。

## Task S4 `[ ]`：统一添加记录与交易流程

**优先级**：P0  
**依赖**：S3  
**主要范围**：`AssetRecordScreen.kt`、`TradeScreen.kt`、OCR/手动录入入口、交易表单组件。

- [ ] 建立统一“添加记录”入口页或底部菜单。
- [ ] 保留截图导入、手动添加资产、记录交易、记录资金变动四个动作。
- [ ] 买入/卖出共用一个交易表单。
- [ ] 资金变动支持收入、大额支出、入金、出金、转账、分红/利息和估值调整。
- [ ] 支持一笔周/月日常支出汇总，不要求逐日补录。
- [ ] 默认只展示必填字段。
- [ ] 次要字段折叠到“更多选项”。
- [ ] 从资产详情进入时自动带入上下文。
- [ ] 统一保存成功、失败和事务回滚反馈。

**完成标准**：用户无需理解多个近义入口即可完成首次录入和后续交易更新。

## Task S5 `[ ]`：重组“我的”与降级低频功能

**优先级**：P1  
**依赖**：S1  
**主要范围**：账户页面、设置页、规划页及数据工具入口。

- [ ] 按“账户 / 数据与安全 / 设置 / 更多工具”重新分组。
- [ ] 数据工具改为紧凑列表行。
- [ ] 将目标配置和策略配置合并。
- [ ] 将规划子功能移入更多工具。
- [ ] 将完整资产变动记录保留在“资产”二级入口，不进入底部导航。
- [ ] 将月度复盘保留为周期性核心入口，同时在更多工具中可主动打开。
- [ ] 将财务报表改为资产变化解释；周期规则仅突出显著收支事项。
- [ ] 隐藏账户首页常驻的合并规则长说明。
- [ ] 账户点击行为统一进入账户详情。

**完成标准**：“我的”首屏以账户和关键数据操作为主，不再像功能陈列页。

## Task S6 `[ ]`：建立低频资产事件与周期复盘闭环

**优先级**：P1  
**依赖**：S2、S4、S5  
**主要范围**：`CashFlowScreen.kt`、`TransactionHistoryScreen.kt`、`FinancialReportScreen.kt`、`RecurringRulesScreen.kt`、`MonthlyReviewScreen.kt`、快照与复盘聚合逻辑。

- [ ] 用户文案从“日常记账/流水”收口为“资产变动/资金变动”。
- [ ] 明确投资交易、外部净流入、内部转账和估值调整的统计口径。
- [ ] 内部转账从净流入中排除。
- [ ] 支持按周/月记录汇总支出。
- [ ] 周期规则默认只推荐工资、房贷、房租、保费等显著事项。
- [ ] 月度复盘计算期初资产、期末资产、净流入、投资收益和估值调整。
- [ ] 数据不足时返回可解释的数据质量状态，不强行凑平归因。
- [ ] 月度复盘首屏生成一句结论，并在无偏离时显示“无需调整”。
- [ ] 每日行情同步不发送交易建议或每日复盘提醒。

**完成标准**：用户无需逐日记账，也能通过少量重要事件在月度复盘中理解资产为什么变化。

## Task S7 `[ ]`：统一视觉密度和组件规范

**优先级**：P1  
**依赖**：S2～S6  
**主要范围**：`ui/components/FinUi.kt`、`ui/theme/Theme.kt`、核心页面。

- [ ] 收敛页面边距、间距、圆角和按钮高度。
- [ ] 新增/调整紧凑列表行、摘要区、设置行、空状态组件。
- [ ] 减少纯白大按钮和多层卡片。
- [ ] 统一选中态、主色和三桶颜色职责。
- [ ] 统一金额、收益率、币种与日期格式。
- [ ] 检查字体放大后的截断和按钮可读性。

**完成标准**：核心页面使用同一套紧凑组件，视觉层级不超过两层。

## Task S8 `[ ]`：数据质量提示与空状态

**优先级**：P1  
**依赖**：S2、S3、S6  

- [ ] 建立异常收益率判定和提示。
- [ ] 将缺汇率、旧价格、缓存价格、待确认代码聚合为统一状态。
- [ ] 总览只显示最高优先级问题，详情展示具体处理方式。
- [ ] 所有列表和报表补齐简洁空状态。
- [ ] 隐藏没有数据支撑的筛选、趋势和零值卡片。

**完成标准**：异常数据不被当作普通成果展示；所有空页面都有明确下一步。

## Task S9 `[ ]`：回归、可用性与范围验收

**优先级**：P0  
**依赖**：S1～S8  

- [ ] JVM 单测与数据库迁移测试通过。
- [ ] Debug/Release 构建、Lint、AndroidTest 编译通过。
- [ ] 模拟器完成首次录入、查看资产、记录交易、记录资金变动、月度复盘、修改目标、备份恢复核心流程。
- [ ] 验证每日价格同步只更新数据，不自动生成买卖建议或每日复盘提醒。
- [ ] 验证周/月汇总支出、内部转账和投资收益在复盘口径中不会混淆。
- [ ] 验证原有本地数据升级后仍可查看和编辑。
- [ ] 验证被降级功能仍可达且返回正常。
- [ ] 使用大字体、窄屏、空数据、单资产、多账户、多币种进行 UI 验收。
- [ ] 对照本文件逐项完成截图复核。

**完成标准**：核心路径更短，低频记录可以解释月度变化，数据无回归，且没有因入口迁移产生死页面。

---

## 10. 核心流程验收脚本

### A. 首次使用

1. 创建账户。
2. 点击“添加记录”。
3. 手动添加资产或截图导入。
4. 返回总览。
5. 首屏看到总资产、三桶结构和数据状态。

期望：不需要进入配置、流水或规划页面即可完成核心目标。

### B. 常规查看

1. 打开应用。
2. 查看总资产和重点提醒。
3. 点击资产摘要进入资产列表。
4. 点击某资产查看详情和最近变化。

期望：从冷启动到资产详情不超过 2 次点击。

### C. 更新交易

1. 进入资产详情。
2. 点击“记录交易”。
3. 选择买入或卖出。
4. 填写数量和价格并保存。
5. 返回详情确认数量、成本和最近交易已更新。

期望：无需在“流水”和“持仓”两个一级入口之间切换。

### D. 记录低频资产事件

1. 从资产页点击“添加记录”。
2. 选择“记录资金变动”。
3. 录入一笔工资、大额支出或本月日常支出汇总。
4. 如为账户转账，选择转入账户。
5. 保存后进入资产变动记录确认结果。

期望：不要求补录每日消费；内部转账不改变总资产和净流入。

### E. 月度复盘

1. 准备上月末和本月末资产快照。
2. 准备本月净流入、投资交易、内部转账和估值调整。
3. 打开月度复盘。
4. 查看期初、期末、净流入、投资收益和结构变化。
5. 在无显著偏离时确认页面显示“无需调整”。

期望：复盘先给结论，资产变化能够被解释；数据不足时明确说明，不生成伪精确结果。

### F. 处理异常

1. 构造成本异常、旧价格或缺汇率数据。
2. 总览出现一条最高优先级提醒。
3. 点击提醒进入具体资产或处理页面。
4. 修正数据后提醒消失或降级到下一条。

期望：用户清楚知道问题、影响和下一步。

---

## 11. 发布门槛

以下条件全部满足后，本轮精简改版才可视为完成：

- [ ] 底部导航严格为 3 项。
- [ ] 总览首屏回答三个核心用户问题。
- [ ] 任一资产只有一个主详情入口。
- [ ] 新增与交易动作已统一命名和路径。
- [ ] 低频资产事件和高级规划不占一级入口，但资产事件可从资产页稳定进入。
- [ ] 月度复盘能够解释期初、期末、净流入、投资收益和估值调整。
- [ ] 每日价格更新不产生每日操作要求或具体买卖暗示。
- [ ] 核心页面没有同等级卡片嵌套。
- [ ] 空状态不渲染无意义零值和筛选器。
- [ ] 异常收益、缺汇率和旧价格具有可信提示。
- [ ] 旧数据、备份、迁移和核心计算无回归。
- [ ] 自动化测试、构建门禁和模拟器核心流程全部通过。

---

## 12. 后续评估指标

改版完成后建议记录以下指标，用于决定高级功能是否继续保留：

- 首次录入完成步骤数与完成率。
- 冷启动到查看某项资产详情的点击数。
- 总览重点提醒的点击和处理完成率。
- “更多工具”各入口的真实使用频率。
- 低频资产事件的月度记录人数、每人记录次数及周/月汇总使用比例。
- 月度复盘的打开率、读完率与异常处理完成率。
- 压力测试、落点等高级功能的 30 天使用人数。
- 用户是否因为每日价格更新误以为需要每日交易或每日复盘。
- 用户是否能在 10 秒内说出总资产和主要资产分布。

若低频能力持续缺乏使用，应优先删除维护成本，而不是重新提高入口层级。
