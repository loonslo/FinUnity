# FinUnity UI/UX Todo

> 目标：把 FinUnity 从“资产记录工具”升级为“靠谱、克制、让人安心的个人财务管理 App”。
> 参考方向：有知有行公开定位强调“投资是为了更好的生活”、心理账户、四笔钱、简洁有章法、信息透明、不过度刺激交易。FinUnity 应借鉴这些产品原则，但不要照搬基金销售场景。
> 本分析基于当前项目代码：`MainActivity.kt`、`MainScreen.kt`、`AccountHubScreen.kt`、`AccountScreen.kt`、`AssetRecordScreen.kt`、`AssetDetailScreen.kt`、`CashFlowScreen.kt`、`SettingsScreen.kt`、`RiskBucketDetailScreen.kt`、`FinUi.kt`、`Theme.kt`、`uxtodolist.md`。

## 当前判断

- 产品底子正确：已有总览、账户、持仓、风险桶、再平衡、交易流水、金额隐藏、CSV 导入、本地存储等核心能力，适合继续向“家庭资产账本 + 配置陪伴”发展。
- 主要短板不是功能数量，而是“解释力”和“可信感”：用户看到金额、风险桶、再平衡建议时，还不够清楚这些数字从哪里来、下一步该做什么、是否可以放心。
- 当前信息架构有混淆：底部 Tab 是“总览 / 资产 / 账户”，但 `资产` 实际展示账户聚合，`账户` 实际更像“我的 / 数据工具”。这会降低新用户理解效率。
- 当前交互偏工具表单：添加账户、添加资产、记一笔、设置目标配置都能完成任务，但缺少渐进式引导、解释文案、保存反馈、撤销、异常状态和空状态闭环。
- 当前视觉风格已向克制金融感靠拢：浅灰背景、白卡、绿色主色、圆角卡片、资产环图是好的方向。但组件、按钮、TopBar、Chip、卡片圆角和颜色使用仍不统一。

## P0 - 产品骨架和可信感

- [x] 重构底部导航命名与职责：建议改为“首页 / 账本 / 我的”，或“总览 / 资产 / 数据”。避免当前 `资产` 对应账户聚合、`账户` 对应工具页的语义错位。
- [x] 首页增加“今日状态 / 安心检查”模块：用 3 条以内结论告诉用户“资产已同步到什么时间、配置是否偏离目标、是否需要操作”。不要只展示图和金额。
- [ ] 首页资产结构卡加入解释入口：点击“进取 / 稳健 / 防守”时，不只进入列表，还要解释该风险维度的含义、适合的钱、建议持有周期、当前偏离情况。
- [x] 把现有风险桶映射为更生活化的心理账户：`防守` 对应活钱或应急钱，`稳健` 对应 1-3 年要用的钱，`进取` 对应 5 年以上不用的钱。保留专业名称，但界面优先讲“这笔钱的用途”。
- [x] 增加“数据来源和更新时间”展示：价格来自 Yahoo Finance、汇率/本位币折算规则、最近同步时间、失败原因都要可见。财务 App 的信任感来自可追溯，而不是只显示漂亮数字。
- [x] 所有保存、删除、转账、卖出、导入动作增加明确结果反馈：优先用 Snackbar，支持“撤销”或“查看流水”。当前多数操作保存后直接跳转，用户不确定是否成功。
- [ ] 删除账户/持仓前展示影响范围：列出会删除的资产记录数、流水数、价格历史数。当前有文字警告，但不够具体，容易造成财务数据误删风险。
- [ ] 为金额隐藏做全局状态：当前 `AccountHubScreen` 内有 `amountsVisible`，应扩展到首页、详情、流水、设置入口，并记住用户选择。

## P0 - 新用户首次体验

- [ ] 设计首次启动流程：第一屏不要直接空状态，应解释“FinUnity 帮你做什么”：统一看资产、按用途分钱、检查配置偏离、保留本地数据。
- [ ] 把“添加账户 -> 添加第一笔资产”做成连续向导：步骤显示为 `1 账户`、`2 资产`、`3 配置用途`、`4 完成`。当前已有 `continueToAsset`，但界面没有步骤感。
- [ ] 空状态改为场景化入口：提供“我有银行卡现金”“我有基金/ETF”“我有股票账户”“我想先导入 CSV”四个入口，而不是只有“添加账户”。（已先优化空状态文案，场景化入口待做）
- [ ] 新手默认配置不要只给百分比输入：提供 3 个模板，如“稳健记账型”“均衡家庭型”“长期投资型”，用户可后续微调。
- [ ] 添加第一笔资产后展示完成页：告诉用户“已记录到哪个账户、属于哪类钱、下一步可以设置目标配置或继续添加”。避免保存后突然回首页。

## P1 - 信息架构与页面职责

- [ ] 首页只回答三个问题：我现在有多少钱、结构是否健康、今天需要做什么。账户列表最多展示 Top 3，其余放到账本页。
- [ ] 账本页按“账户视角”和“资产视角”切换：当前 `AccountHubScreen` 以账户卡为主，但用户也需要按资产类型、风险桶、币种查看。
- [ ] 我的/数据页承载设置、导入、隐私、本地备份、价格同步、免责声明。当前 `AccountAssetsByAccountScreen` 更像工具页，应重命名并补齐数据安全表达。
- [ ] 交易流水页增加筛选：按账户、资产、交易类型、时间范围筛选。财务产品必须让用户能追溯每个数字。
- [ ] 资产详情页重新定义主操作：当前顶部有“买入/卖出”，底部又有“调整持仓”，容易重复。建议主按钮固定为“记一笔”，二级操作为“编辑资料 / 删除”。
- [ ] `CashFlowScreen` 中“投资买卖会统一接入这里”目前是半成品提示。要么接入真实买卖流程，要么暂时隐藏“投资买卖”入口，避免用户认为功能不可用。

## P1 - 表单与录入体验

- [ ] `AssetRecordScreen` 按资产类型拆分表单：现金、基金/ETF、股票、定期存款使用不同字段顺序和说明。当前一个动态表单承载太多规则。
- [ ] 输入字段增加计算说明：例如“当前市值 = 数量 × 当前价格”，“累计盈亏 = 当前市值 - 买入总成本”。让用户理解而不是猜。
- [ ] 金额输入增加格式化和大数字辅助：输入 `100000` 时显示“约 10.00 万 CNY”。财务 App 对大额输入必须降低误输风险。
- [ ] 卖出流程补齐成交价、费用、日期、备注：当前 `showSellDialog` 只输入数量，并用当前价估算，不足以形成可信流水。
- [ ] 账户转账应显示“转出后余额”和“转入后余额”预览，并禁止跨币种时解释原因。当前只过滤同币种账户，没有解释。
- [ ] 所有表单离开前检测未保存改动：点击返回时弹出“保存草稿 / 放弃修改”。当前返回会直接丢失输入。
- [ ] CSV 导入前增加字段映射预览和错误行解释：导入财务数据不能只给成功/失败，需要让用户知道哪几行被跳过。

## P1 - 可视化和资产解释

- [ ] 首页环图中心不要只写“结构 + 金额”：增加“配置健康 / 偏离 X% / 无需调整”等状态语义。
- [ ] 再平衡提醒从“可以考虑增加/减少金额”升级为方案卡：展示当前占比、目标占比、差额、建议优先处理的账户或资产。
- [ ] 价格历史页增加趋势图，而不是纯列表。即使先做简单折线图，也比时间列表更符合用户心智。
- [ ] 资产详情增加“收益贡献”解释：区分价格变化、现金流、汇率影响。多币种产品如果不解释汇率，用户会不信任收益数字。
- [x] `AccountHubScreen` 里“加权收益率”和“年化收益率”当前显示 `--`，上线前应隐藏或补齐计算。长期占位会损害专业感。
- [ ] 风险桶详情页把“旧持仓”概念替换成用户能理解的名称。代码里 `旧持仓` 会让用户困惑，应统一为“证券持仓”或“历史持仓（迁移数据）”。

## P1 - 视觉系统

- [ ] 收敛颜色 Token：当前 `Theme.kt`、`FinUi.kt`、各页面仍有硬编码白色、绿色、红色、灰色。统一到 `FinColors` 和 `MaterialTheme.colorScheme`。
- [ ] 统一按钮语义：主操作、次操作、危险操作、文本操作分别定义组件。当前 `Button`、`FinSoftButton`、`OutlinedButton`、`TextButton` 分散使用，视觉权重不稳定。
- [ ] 统一顶部栏：返回页的 `TopAppBar` 有的有标题、有的空标题、有的背景不同。建议封装 `FinTopBar(title, onBack, actions)`。
- [ ] 统一卡片规格：主要信息卡用 24/28dp 圆角，列表卡用 16/20dp，表单卡用 20/24dp。当前 `RoundedCornerShape(10/12/16/18/24/28)` 混用。
- [ ] 增加“靠谱金融感”的细节：更克制的阴影、分割线、数字等宽字体、金额小数弱化、币种标签统一位置。
- [ ] 图标体系不要用文字替代：如 `CSV`、`Set`、`F`、`+` 可以作为临时方案，但正式界面应统一使用图标或品牌化插画。

## P1 - 文案与信任表达

- [ ] 建立文案原则：少刺激交易，多解释用途；少说收益，多说计划；少制造焦虑，多给下一步。
- [ ] 所有风险相关词补充解释：进取、稳健、防守、再平衡、阈值、目标配置都需要短说明。
- [ ] 设置页加入“这些设置会影响什么”：本位币、目标配置、再平衡阈值改变后，会影响哪些页面数字。
- [ ] 删除“实时价格”这种绝对表达，改成“自动同步价格 / 最近同步”。Yahoo Finance 和后台任务不保证真正实时。
- [ ] 错误提示从技术错误改成行动建议：例如“价格同步失败”后给出“稍后重试 / 手动输入价格 / 查看上次价格”。

## P2 - 高级但重要的体验

- [ ] 增加本地备份和恢复入口：既然 README 强调本地数据，必须给用户迁移手机和防丢数据的方案。
- [ ] 增加隐私锁：支持进入 App 时验证系统生物识别或 PIN。财务 App 显示个人资产，金额隐藏不够。
- [ ] 增加资产目标：例如买房、教育、养老、应急金。让资产配置和生活目标建立联系，而不只是账户和资产列表。
- [ ] 增加定期回顾：月度资产变化、现金流、配置偏离、最大收益/亏损贡献。已有 `HistoryScreen` 和快照能力，可以包装成“月报”。
- [ ] 增加提醒策略：配置偏离、价格同步失败、长期未更新手动资产、现金低于应急目标时提醒。
- [ ] 支持家庭成员/标签：账户或资产可标记为本人、配偶、孩子、家庭共同。财务管理 App 常见真实场景是家庭资产。

## 建议实施顺序

- [ ] 第 1 周：重命名底部导航，封装 `FinTopBar`、统一按钮组件、隐藏未实现的 `--` 指标、补齐保存/删除 Snackbar。
- [ ] 第 2 周：重做首次启动和添加第一笔资产向导，优化空状态和表单说明。
- [ ] 第 3 周：重做首页“安心检查”和风险桶解释，把风险桶升级为心理账户表达。
- [ ] 第 4 周：补齐交易流水筛选、卖出/转账预览、数据来源与同步时间。
- [ ] 第 5 周：做价格趋势图、月度回顾、本地备份、隐私锁。

## 参考来源

- 有知有行官网关于定位、心理账户和四笔钱框架：https://youzhiyouxing.cn/about/
- App Store 公开介绍中关于“简洁、有章法”“没有基金排行榜/热点推荐/大而全基金超市”“信息更清晰、更透明”“心理账户”的描述：https://apps.apple.com/cn/app/id1513319754
