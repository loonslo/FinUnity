# 资产配置规划 SOP — 产品需求文档（PRD v1.2）

> 基于：资产配置规划标准作业流程（SOP v2026-06-06）
> 修订：v1.2（2026-06-06）在 v1.1 基础上修复 6 项遗留问题
> 目标：Skill 先行 → Web App 后做
> 计算深度：引导 + 模板（用户手动填数，不含实时行情）
> 不构成投资建议

---

## 变更说明（v1.1 → v1.2）

| # | 遗留问题 | 修复内容 |
|---|---|---|
| A | **三层比例 → 7 桶目标金额的拆分规则缺失**：步骤 3 只输出防守/稳健/进取三层百分比，但步骤 4/6/8 都依赖 7 桶（B1–B7）的目标金额，从三层到七桶如何拆分全文未定义，导致 `buckets_target` 无法计算 | 新增 **2.6 节**：定义每层内部的默认拆分比例（用户可调），打通三层→七桶 |
| B | **生存层与 B1 现金底线概念重叠、有双算风险**：步骤 2 把"生存层 = 月开支×月数"从策略盘中扣除；步骤 3 又在策略盘内设"B1 现金底线 = N 个月开支"。两笔都用"月开支×月数"口径，易被理解为同一笔钱算两次 | 新增 **2.7 节**：明确两者为相互独立的两个资金池，口径与用途分离，B1 底线改用策略盘占比/绝对额定义 |
| C | **步骤 3 反推法与步骤 7 压测的 M 校验口径不一致**：步骤 3 用单一熊市跌幅（如 55%）对全部权益反推；步骤 7 用各桶差异化跌幅（成长桶可达 −80%）精算。两者可能一个通过一个不通过，造成 3↔7 反复横跳 | 修订 **步骤 3 / 步骤 7 行为规范**：明确定位为"初筛—复核"两道闸，说明二者必然存在口径差异及处理顺序，并建议初筛取偏保守跌幅以降低复核不通过概率 |
| D | **缺口率除零**：缺口率 = (目标 − 现有) / 目标，当某桶目标 = 0（用户不配该桶）时除零；边界测试未覆盖 | 修订 **步骤 4 计算规则**与 **6.4 边界测试**：定义目标 = 0 时的缺口率取值与展示规则 |
| E | **step7 数据模型 B7 跌幅硬编码，与 2.3「B7 用户自填」矛盾**：4.4 的 scenarios 把 B7 写死了具体数值，而 2.3 注明 B7 由用户自填 | 统一为：2.3 给出 B7 默认值（= B5），数据模型注明该值为**默认预填**、用户可改 |
| F | **多用户前缀与单 vault 路径混用未说明**：3.5 既说文件名加用户前缀，又假设单用户 vault 目录，多用户场景下路径归属不清 | 修订 **3.5**：明确多用户时以 owner 子目录隔离，单用户沿用默认路径 |

> v1.0 → v1.1 的 14 项修复记录见附录 A（保留存档）。

---

## 一、目标

把 10 步 SOP 做成两层工具：

| 层 | 形态 | 核心价值 |
|---|---|---|
| Phase 1 | Claude Skill | 对话式引导，逐步收集输入，生成个人化方案文档 |
| Phase 2 | Web App | 表单 + 自动计算 + 可视化，输出可打印报告 |

---

## 二、核心数据定义

> 本节集中定义全文复用的常量和规则，避免各步骤分散定义造成不一致。

### 2.1 细粒度桶定义（步骤 4、6、8 共用）

| 桶 ID | 名称 | 归属层/账户 | 说明 |
|---|---|---|---|
| B1 | 现金 | 防守层 | 活期/货基，可随时取用 |
| B2 | 弹药 | 防守层 | 专用于回撤加仓，非日常周转 |
| B3 | 核心宽基权益 | 稳健层 | 宽基指数，低跟踪误差，长期持有 |
| B4 | 价值/红利/低波权益 | 稳健层 | 防御型权益，关注估值区间 |
| B5 | 成长/科技权益 | 进取层 | 高波动，必须设比例上限 |
| B6 | 境外权益通道 | 进取层 | 币种/市场分散工具 |
| B7 | 训练仓/主题卫星 | 进取层 | 小比例，不向下摊平，写明退出条件 |

> **注**：用户可根据实际持仓增减子桶（如将 B3 拆分为 A股宽基/港股宽基），但默认框架为以上 7 桶。

> **层映射**：防守层 = {B1, B2}；稳健层 = {B3, B4}；进取层 = {B5, B6, B7}。步骤 3 输出的"防守% / 稳健% / 进取%"即按此映射拆到桶（拆分规则见 2.6）。

### 2.2 步骤 3 经验法调整系数（量化）

| 条件 | 调整幅度 |
|---|---|
| 基线 | 65% |
| 收入稳定且有持续结余 | +5% |
| 现育儿 | −5% |
| 计划 1 年内育儿 | −5% |
| 收入不稳定（自雇/合同工/待业） | −5% |
| 负债较重（月还款 > 收入 30%） | −5% |

> 取反推法和经验法结果中**更保守（更低）者**为最终权益比。
> 进取层占比 = 最终权益% 中分配给进取层的部分；防守层 = 100% − 稳健层% − 进取层%。三层的具体落位见 2.6。

### 2.3 步骤 7 历史情景参考跌幅

> **免责**：以下为参考区间值，非精确回测数据，不含税费/汇率/择时，过去不代表未来。用户可自行修改。

| 情景 | B3 核心宽基 | B4 价值低波 | B5 成长科技 | B6 境外权益 | B7 训练仓（默认=B5） | B1 现金/B2 弹药 |
|---|---|---|---|---|---|---|
| 2022 科技回调 | −30% | −15% | −55% | −35% | −55% | 0% |
| 2020 新冠闪崩 | −35% | −25% | −40% | −35% | −40% | 0% |
| 2008 系统性危机 | −55% | −40% | −60% | −50% | −60% | 0% |
| 2000 科技泡沫 | −45% | −20% | −80% | −45% | −80% | 0% |

> B7 训练仓默认跌幅 = 同情景 B5 值（已预填于上表及数据模型），用户可改为更深值。

### 2.4 步骤 6 加仓阶梯默认参数

仅适用于 B3 核心宽基权益（低杠杆、低费率、可长期持有）：

| 回撤深度 | 资金来源 | 投入比例（相对 1 份） |
|---|---|---|
| −5% ～ −10% | 提高定投 | 1 |
| −10% ～ −20% | 定投 + 弹药 | 1.5 |
| −20% ～ −30% | 弹药为主 | 2 |
| > −30% | 弹药 + 备用通道 | 2.6 |

> 各档之和 = 弹药（B2）总额度。前提：B1 现金 ≥ 底线后才动手。

### 2.5 步骤 8 每月瀑布决策树

```
每月结余到账后，按以下顺序判断：

IF B1 现金 < B1 现金底线（步骤3，见2.7）:
  → 100% 补 B1，停一切权益新增
  → 结束

ELSE IF 步骤6中任意回撤信号已触发:
  → 按触发档位执行加仓阶梯（定投 + B2 弹药按比例投入 B3）
  → 结束

ELSE IF 防守层（B1+B2）< 防守层目标金额（2.6 推算）:
  → 100% 补 B2 弹药至目标
  → 结束

ELSE:
  → 计算各桶缺口率 = (目标 − 现有) / 目标   （目标=0 的桶按 2.4 规则跳过，见步骤4计算规则）
  → 投入缺口率最大的桶
  → 若多桶缺口率相近（差值 < 5 个百分点），按 B3→B4→B5→B6 优先级排序

大额到账（理财到期等）：
  先确认 B1 ≥ 底线 → 再确认 B2 满 → 余额进"权益建设池"
  按 12–18 个月分批投入，权重向缺口率最大的桶倾斜
  高位（估值贵）则暂放货基按月慢投，不追高
```

### 2.6 三层比例 → 7 桶目标金额拆分规则【v1.2 新增】

> 解决：步骤 3 只产出三层百分比，而步骤 4/6/8 需要 7 桶各自的目标金额。本节定义默认拆分，用户可在步骤 5 调整每桶上限时一并覆盖。

**第一步：三层金额**
```
防守层金额 = 防守% × 策略盘
稳健层金额 = 稳健% × 策略盘
进取层金额 = 进取% × 策略盘
```

**第二步：层内拆分到桶（默认比例，用户可调）**

| 层 | 桶 | 默认层内占比 | 约束 |
|---|---|---|---|
| 防守层 | B1 现金 | B1 目标 = B1 现金底线（2.7 定义），优先满足 | 不参与百分比分配，取绝对额 |
| 防守层 | B2 弹药 | = 防守层金额 − B1 目标 | 若结果 < 0，提示防守层比例不足以覆盖现金底线，回步骤 3 |
| 稳健层 | B3 核心宽基 | 60% | 用户可调，B3+B4 = 100% |
| 稳健层 | B4 价值低波 | 40% | 同上 |
| 进取层 | B5 成长科技 | 50% | **必须设比例上限**（步骤 5），默认 ≤ 进取层 60% |
| 进取层 | B6 境外权益 | 40% | 受境外额度约束（步骤 6） |
| 进取层 | B7 训练仓 | 10% | 硬上限 = 策略盘 5%，超出部分回拨 B5/B6 |

**校验**：7 桶目标金额之和必须等于策略盘（允许 ±1 元舍入）。Skill/App 在步骤 3 输出后即展示该拆分表，并校验合计。

> 设计意图：先按"底线优先"锁定 B1，再把防守层余额给 B2 弹药，避免现金底线和弹药互相挤占；稳健/进取层用固定默认比例保证可复现，同时留出用户覆盖口子。

### 2.7 生存层与 B1 现金底线的关系【v1.2 新增 / 消除双算】

> 解决：步骤 2 的"生存层"与步骤 3 的"B1 现金底线"都曾用"月开支×月数"口径，易被当成同一笔钱算两次。

| 资金池 | 所在 | 口径 | 用途 | 是否计入策略盘 |
|---|---|---|---|---|
| **生存层** | 策略盘**之外** | 月刚性开支 × 保守月数（步骤 2） | 应急生存、不参与任何配置与加仓 | **否**（已从总资产扣除） |
| **B1 现金底线** | 策略盘**之内**（防守层） | 默认 = 策略盘 × 5%，或用户指定绝对额；**不再用"月开支×月数"口径** | 配置周转 + 加仓动手前的流动性缓冲 | **是** |

规则：
- 两池**物理与口径均独立**，不重叠、不相互替代；展示时分别列示，避免合并。
- 加仓阶梯（2.4）动用的是 B2 弹药，前提是 **B1 ≥ B1 现金底线**；生存层任何时候都不得动用。
- 若用户坚持用"月数"定义 B1 底线，Skill 必须显式提示："该口径与生存层重叠，请确认两者是否为同一笔钱，避免重复计提。"

---

## 三、Phase 1 — Claude Skill

### 3.1 触发词

```
"开始资产配置"
"帮我做资产配置方案"
"走一遍资产配置 SOP"
```

### 3.2 整体流程

```
invoke skill
  └─ 步骤 0：输出原则确认 → 用户确认继续
  └─ 步骤 1：收集全口径资产 → 生成资产表
  └─ 步骤 2：分三层 → 确认策略盘金额（生存层独立列示，见2.7）
  └─ 步骤 3：收集 M 和约束 → 输出攻防比 → 按2.6拆出7桶目标金额
  └─ 步骤 4：持仓映射到7桶 → 生成缺口表
  └─ 步骤 5：资产类别偏好 → 输出筛选标准（无具体荐股）
  └─ 步骤 6：生成信号→应对表（含加仓阶梯默认参数）
  └─ 步骤 7：压力测试（分桶精算复核）→ 验证 M 约束
  └─ 步骤 8：输出落地执行表（含每月瀑布决策树）
  └─ 步骤 9：输出复盘模板 + 版本说明
  └─ 汇总：生成完整方案 .md 文件
```

### 3.3 每步 Skill 行为规范

**步骤 0（原则确认）**
- 输出固定原则清单（直接展示，无需用户填写）
- 必须包含：只应对不预测 / 区分事实-推理-假设-观点 / 红线清单 / 不构成投资建议
- 用户输入 `确认` 后才进入步骤 1；不可跳过

**步骤 1（全口径资产）**
- 提供账户分类模板（银行/支付宝/券商A/券商B/海外券商/港卡/黄金/现金）
- 用户逐行填入：平台、项目名、币种、金额
- Skill 计算：外币 × 用户提供汇率 → 折本币合计
- 自检：逐项加总 == 各平台总额（Skill 计算并展示差额）
- 输出格式：

```
| 平台 | 项目 | 币种 | 原币金额 | 汇率 | 折本币 |
截至日期：____  数据来源：用户自填
```

**步骤 2（三层分层）**
- 输入：月刚性开支、保守月数（3-6）、是否设深层储备（是/否，金额）
- 计算：生存层 = 月开支 × 月数；策略盘 = 总资产 − 生存层 − 深层储备
- 输出：三个数字 + 说明（生存层明细列表）
- **明确**：生存层为策略盘之外的独立资金池（见 2.7），后续 B1 现金底线不与之重叠

**步骤 3（攻防比）**
- 输入：年龄、家庭阶段、收入稳定性、负债情况、M（最大可承受浮亏金额）
- 输入：熊市跌幅假设（默认 55%，用户可选 45% / 50% / 55%）
- 计算（展示过程）：
  - 反推法（**初筛**）：权益上限 = M ÷ (策略盘 × 跌幅假设)
  - 经验法：基线 65% ± 阶段调整（按第二章 2.2）
  - 最终权益% = min(反推法, 经验法)
- **初筛定位说明**：此处用单一熊市跌幅对全部权益做快速上限估算，是粗筛；步骤 7 会用各桶差异化跌幅做精算复核，二者口径不同属正常。**建议跌幅假设取偏保守值（55%）**，以降低步骤 7 复核不通过、来回返工的概率。
- 输出：防守 X% / 稳健 Y% / 进取 Z%（各自金额）+ 计算过程展示
- 输出：B1 现金底线金额（默认 = 策略盘 × 5%，可改绝对额；**不用月开支×月数口径**，见 2.7）
- 输出：**按 2.6 拆出的 7 桶目标金额表**，并校验合计 = 策略盘

**步骤 4（现状映射）**
- 用步骤 1 的每笔持仓，请用户逐笔归类到 **7 个细粒度桶**（B1-B7，见第二章 2.1）
- Skill 计算每桶：现有金额 / 目标金额（来自步骤 3 / 2.6）/ 缺口 / 缺口率
- **缺口率计算规则（含除零保护）**：
  - 目标 > 0：缺口率 = (目标 − 现有) / 目标
  - 目标 = 0 且 现有 > 0：标记为"超配/计划外持有"，缺口率显示为 `—`（不参与排序），并触发结构病"超配"
  - 目标 = 0 且 现有 = 0：缺口率显示为 `—`（该桶不适用）
- 结构病检测规则：
  - 超配：某桶现有 > 目标 × 110%（目标=0 时只要现有>0 即超配）
  - 不足：某桶现有 < 目标 × 90%（目标>0 时适用）
  - 碎片：同一桶内持有 ≥ 3 只相同风格产品
  - 重复暴露：两只产品底层资产重叠度高（用户人工判断，Skill 提示风险）
- 输出：缺口表（含缺口率排序，`—` 项置底） + 结构病清单

**步骤 5（资产类别）**
- 展示 7 桶 × 资产类别框架（对应第二章 2.1）
- 用户为每桶选择资产类别、填入比例上限和禁买条件（可在此覆盖 2.6 默认层内比例）
- 输出：每桶筛选标准卡片
- **禁荐股实现**（见 3.4）

**步骤 6（信号→应对）**
- 展示 8 类预填信号框架（回撤/泡沫/再平衡/估值/境外额度/生活/限购/单一高风险权益）
- B3 加仓阶梯预填默认参数（第二章 2.4），用户可修改各档阈值和资金比例
- 用户填入自己的具体触发阈值
- 输出：完整信号→动作表

**步骤 7（压力测试 — 精算复核）**
- 展示第二章 2.3 的参考跌幅表（并注明免责声明）
- 用户确认或修改各情景跌幅（含 B7，默认 = B5）
- 用步骤 4 的各桶权重自动计算：组合峰谷 ≈ Σ(桶权重 × 该情景跌幅)
- 展示：各情景最坏浮亏金额 vs M
- **与步骤 3 的关系**：步骤 3 是单一跌幅初筛，本步是分桶精算复核。二者口径不同，可能出现"步骤 3 通过、步骤 7 不通过"（常因成长桶 B5 跌幅 −80% 深于初筛假设）。此时以**步骤 7 为准**。
- **硬校验**：若任一情景浮亏 > M → 展示警告，提示回步骤 3 调低权益%（或下调 B5/B7 占比）或提高 M；返工后重跑步骤 7
- 输出：情景压测表 + 长期期望年化（仅参考，附免责说明）

**步骤 8（落地执行）**
- 根据前序数据自动填入：本周清单（对应步骤 2 的生存层隔离操作）
- 展示第二章 2.5 的每月瀑布决策树（用用户数据填充）
- 用户填入：月结余金额、大额到账金额（可选）
- 自动生成：每月投入建议金额分配（各桶缺口率排序，`—` 桶不参与）
- 输出：本周清单 / 每月瀑布 / 到账分配 三张表

**步骤 9（复盘模板）**
- 输出月/季/年复盘 checklist（无需用户输入）
- 版本管理说明（适配用户 vault 结构，见 3.5）

**汇总输出**
- 合并全部步骤输出为一个完整 `.md` 文件
- 文件名：`资产配置方案-v1.0-YYYYMMDD.md`（多用户加 owner 前缀，见 3.5）
- 保存路径：见 3.5
- 多方案说明：同一用户的多份方案用 `-v{N}` 区分（见 3.5）

### 3.4 禁荐股约束实现规范

Skill prompt 必须包含以下控制机制：

**Negative instruction（明确禁止）**
```
禁止在步骤5、步骤8的输出中出现：
- 任何具体股票名称、代码（A股/港股/美股）
- 任何具体基金名称、基金代码（如 510300、QDII-xxx）
- 任何ETF、LOF、FOF、指数增强基金的具体产品名
- 任何"推荐XX"、"可以买YY"等建议性表述
```

**允许的输出形式**
```
✅ "宽基权益类（追踪沪深300或全A），筛选：规模>50亿、跟踪误差<0.5%、费率<0.15%"
✅ "候选产品由用户自行查找并在个案方案中二次确认"
❌ "可以考虑易方达沪深300ETF"
❌ "推荐510300"
```

**验证步骤**：每次步骤 5 和步骤 8 输出前，Skill 执行自我检查：输出中是否包含具体产品名或代码？如有，删除并替换为类别描述。

### 3.5 版本管理规范（适配用户 vault）

```
默认（单用户）：
  当前版本：wiki/topics/资产配置方案-v{N}-YYYYMMDD.md  （标记 🔄）
  历史版本：archive/资产配置方案-v{N-1}-YYYYMMDD.md    （标记 📦）
  index 登记：wiki/index.md 中维护当前版本路径

多用户：以 owner 子目录隔离，避免前缀与单 vault 假设混淆：
  当前版本：wiki/topics/{owner}/资产配置方案-v{N}-YYYYMMDD.md
  历史版本：archive/{owner}/资产配置方案-v{N-1}-YYYYMMDD.md
  （owner 取 meta.owner 字段；为空时退回默认单用户路径）

文件内必须包含：
  - 版本号（v1.0, v1.1, ...）
  - 相对上一版改了什么（diff 说明）
  - 生成日期

多方案管理：
  - 同一用户不同时期的方案：v1.0, v2.0, ...（大版本 = 攻防比发生调整）
  - 同版本小修订：v1.1, v1.2, ...（小版本 = 阈值、产品微调）
```

### 3.6 Skill 文件结构

> ⚠️ Cowork skill 系统以 **SKILL.md 单文件**为标准格式。步骤引导逻辑、模板、红线规则均内嵌在 SKILL.md 中，不依赖外部文件引用。若未来 skill 系统支持多文件，可拆分；当前版本不拆分。

```
skills/asset-allocation/
└── SKILL.md    # 包含：触发词 + 10步引导逻辑 + 所有模板 + 红线规则
```

SKILL.md 内部分节结构：
```markdown
# Asset Allocation SOP Skill
## 触发词
## 步骤引导逻辑（0-9）
## 模板库
  ### T1 全口径资产表
  ### T2 三层→7桶目标拆分表（2.6）
  ### T3 7桶缺口表
  ### T4 信号→应对表（含阶梯默认参数）
  ### T5 压力测试表（含情景跌幅）
  ### T6 执行表（含瀑布决策树）
  ### T7 完整方案输出格式
## 约束规则
  ### 红线
  ### 禁荐股规则
  ### 不可跳步规则
```

### 3.7 Skill 约束

| 约束 | 规则 |
|---|---|
| 红线 | 下单/调仓/连账户/改持仓记录 → 不执行，说明触发点后停止 |
| 产品建议 | 步骤5和步骤8不得输出具体股票/基金代码（见3.4实现规范） |
| 数据标注 | 每个财务结论标注截至日期 + 数据来源 + 置信度 |
| 回测声明 | 步骤7必须展示免责声明，且紧跟压测结果呈现 |
| 不可跳步 | 步骤3（M）未完成 → 不得进入步骤7；步骤1未完成 → 不得进入步骤4；步骤3未产出7桶目标 → 不得进入步骤4 |
| 步骤回退 | 见3.8 |

### 3.8 步骤回退处理规则

| 回退场景 | 处理方式 |
|---|---|
| 步骤4-9 → 回退到步骤1（修改资产金额） | 步骤2-9 数据全部清除，从步骤2重新计算 |
| 步骤4-9 → 回退到步骤2（修改生存层） | 步骤3-9 数据全部清除 |
| 步骤4-9 → 回退到步骤3（修改M或攻防比） | 步骤4-9 数据全部清除（含 2.6 拆分的 7 桶目标）；Skill 提示"攻防比变更，后续步骤需重新填写" |
| 步骤7-9 → 回退到步骤6（修改信号阈值） | 仅步骤8-9 数据清除；步骤7 压测结果保留但标注"参数已变更，建议重跑" |
| 步骤9 → 回退到步骤8 | 仅步骤9 数据清除 |

> **Skill 实现**：对话中回退时，Skill 明确告知用户哪些步骤数据会被清除，确认后才执行。

---

## 四、Phase 2 — Web App

### 4.1 技术选型

- 单文件 HTML + React（可在 Cowork 直接运行，无需部署）
- 跨会话持久化：JSON 文件导出/导入（不使用 localStorage）
- 计算层：纯前端 JS，无后端，无外部 API

### 4.2 页面结构

```
Header：当前步骤 / 总进度（步骤 X/10）
Sidebar：步骤导航（已完成✓ / 当前→ / 待完成○ / 锁定🔒）
Main：当前步骤的表单 + 说明
Footer：上一步 / 下一步（锁定条件未满足时禁用） / 保存草稿
```

### 4.3 各步骤 UI 规格

| 步骤 | UI 类型 | 关键计算 | 锁定条件 |
|---|---|---|---|
| 0 | 原则展示 + 全部勾选框 | 无 | 全部框勾选 |
| 1 | 动态行表格（增删行）+ 汇率输入 | SUM + 外币换算，展示差额 | 合计差额 = 0 |
| 2 | 3 个数字 input + 月数 slider | 自动算策略盘（生存层独立展示） | 策略盘 > 0 |
| 3 | 5 个 input/radio + M + 跌幅假设 | 双公式取 min，**按 2.6 算 7 桶目标金额并校验合计** | M > 0 且攻防比合理 且 7桶合计=策略盘 |
| 4 | 持仓归类下拉（选7桶之一） + 自动缺口计算 | 缺口 = 目标 − 现有，缺口率排序（除零保护），结构病 flag | 所有持仓已归类 |
| 5 | 7桶 × 资产类别多选卡片 + 上限 input | 无计算，规则约束（禁荐股）；可覆盖 2.6 层内比例 | 每桶至少选一类 |
| 6 | 8类信号预填表 + 阈值 input（B3阶梯预填） | 无计算 | 所有阈值已填 |
| 7 | 情景跌幅可编辑表 + 自动回撤计算 | Σ(桶权重×跌幅)，对比M，M不通过禁用下一步 | M校验通过 |
| 8 | 自动生成3张表（瀑布决策树可视化）+ 可编辑 | 依赖前序数据 | 月结余已填 |
| 9 | 复盘checklist展示 + 导出 | 无 | 无（直接可导出） |

### 4.4 数据模型（前端 State）

```javascript
{
  schema_version: "1.2",          // 用于草稿导入时的版本兼容检查
  meta: {
    version: "v1.0",              // 方案版本号
    date: "YYYY-MM-DD",
    currency_base: "CNY",
    owner: "",                     // 用户标识（用于多方案/多用户路径区分）
    plan_name: ""                  // 方案名（如"2026年主方案"）
  },
  step0: { confirmed: false },
  step1: {
    accounts: [{ platform, item, currency, amount, rate, local_amount }],
    total_local: 0,
    as_of_date: ""
  },
  step2: {
    monthly_expense: 0,
    survival_months: 6,
    deep_reserve: 0,
    survival_total: 0,            // 策略盘之外的独立池（见2.7）
    strategy_total: 0
  },
  step3: {
    age: null,
    family_stage: "",              // "育儿中" | "计划育儿" | "无"
    income_stable: null,           // true | false
    heavy_debt: null,              // true | false
    M: 0,
    bear_drop_assumption: 0.55,    // 用户可选 0.45 | 0.50 | 0.55（初筛用）
    equity_pct_inferred: 0,        // 反推法结果（初筛）
    equity_pct_experience: 0,      // 经验法结果（含各阶段调整明细）
    equity_pct_final: 0,           // min(inferred, experience)
    layer_pct: { defense: 0, steady: 0, growth: 0 },  // 三层占比
    cash_floor: 0,                 // B1现金底线，默认=strategy_total*0.05（见2.7，不用月数口径）
    // 2.6 层内默认拆分比例（用户可在step5覆盖）
    split: {
      steady: { B3: 0.6, B4: 0.4 },
      growth: { B5: 0.5, B6: 0.4, B7: 0.1 }   // B7 另受 策略盘*5% 硬上限约束
    },
    buckets_target: {              // 7桶目标金额（由 layer_pct + split + cash_floor 推算，2.6）
      B1: 0, B2: 0, B3: 0, B4: 0, B5: 0, B6: 0, B7: 0
    },
    buckets_target_sum_check: 0    // 应 == strategy_total（±1元）
  },
  step4: {
    positions: [{ item, bucket_id, current_amount }],
    buckets_current: { B1: 0, B2: 0, B3: 0, B4: 0, B5: 0, B6: 0, B7: 0 },
    buckets_gap: { B1: 0, B2: 0, B3: 0, B4: 0, B5: 0, B6: 0, B7: 0 },
    // 缺口率：目标>0 时为数值；目标=0 时为 null（UI 显示 "—"，不参与排序）
    buckets_gap_rate: { B1: 0, B2: 0, B3: 0, B4: 0, B5: 0, B6: 0, B7: 0 },
    structure_issues: []           // [{ type: "超配"|"不足"|"碎片"|"重复", bucket, detail }]
  },
  step5: {
    buckets: [{
      bucket_id, asset_class, criteria, cap_pct, forbidden: []
    }]
  },
  step6: {
    signals: [{
      type, trigger_condition, action
    }],
    ladder: {                      // B3加仓阶梯
      tiers: [
        { drop_from: -0.05, drop_to: -0.10, source: "定投", multiplier: 1 },
        { drop_from: -0.10, drop_to: -0.20, source: "定投+弹药", multiplier: 1.5 },
        { drop_from: -0.20, drop_to: -0.30, source: "弹药为主", multiplier: 2 },
        { drop_from: -0.30, drop_to: null,  source: "弹药+备用通道", multiplier: 2.6 }
      ]
    }
  },
  step7: {
    // B7 为默认预填值（=同情景 B5），用户可改 —— 与 2.3 一致
    scenarios: [
      { name: "2022科技回调", drops: { B3: -0.30, B4: -0.15, B5: -0.55, B6: -0.35, B7: -0.55, B2: 0, B1: 0 } },
      { name: "2020新冠闪崩", drops: { B3: -0.35, B4: -0.25, B5: -0.40, B6: -0.35, B7: -0.40, B2: 0, B1: 0 } },
      { name: "2008系统性",   drops: { B3: -0.55, B4: -0.40, B5: -0.60, B6: -0.50, B7: -0.60, B2: 0, B1: 0 } },
      { name: "2000科技泡沫", drops: { B3: -0.45, B4: -0.20, B5: -0.80, B6: -0.45, B7: -0.80, B2: 0, B1: 0 } }
    ],
    results: [{ scenario_name, loss_pct, loss_abs }],
    worst_loss_abs: 0,
    m_check_pass: false
  },
  step8: {
    monthly_surplus: 0,
    lump_sum_incoming: 0,
    this_week: [],
    monthly_cascade: [],           // 依据2.5决策树生成
    lump_sum_plan: []
  },
  step9: {
    version_notes: "",
    review_checklist: {}
  }
}
```

**版本兼容规则**：导入草稿时检查 `schema_version`；若版本不匹配显示警告并提示用户检查字段（v1.1 草稿缺少 `step3.layer_pct / split / buckets_target_sum_check`，导入后需在步骤 3 重新生成 7 桶目标）。

### 4.5 输出

- **导出 Markdown**：完整方案文档（同 Skill 输出格式，保存路径见 3.5）
- **导出 PDF**：浏览器 print-to-PDF（页面 CSS 需含 `@media print` 样式）
- **保存草稿**：JSON 文件下载（含 schema_version 字段），下次可导入恢复

### 4.6 红线 & 边界

同 Skill 约束（3.7），额外：
- 步骤 3 7 桶目标合计 ≠ 策略盘 → "下一步"禁用 + 提示合计差额
- 步骤 7 M 校验不通过 → "下一步"按钮禁用 + 红色提示 "最坏浮亏（X万）> M（Y万），请返回步骤3调低权益比例或下调 B5/B7"
- 所有结果页底部固定显示免责声明
- 回退时遵守 3.8 规则：清除相关步骤数据前弹出确认 Dialog

---

## 五、实施路径

### Phase 1（Skill）— 预计工时

| 任务 | 内容 | 估计 |
|---|---|---|
| T1 | 写 SKILL.md（10步引导逻辑 + 计算规则 + 2.6/2.7 拆分逻辑 + 所有模板内嵌） | **7–9h** |
| T2 | 写并验证禁荐股规则（negative instruction + 测试用例） | 1h |
| T3 | 走完整流程测试（用真实数字，覆盖正常路径，校验 7 桶合计） | 1.5h |
| T4 | 测试边界和回退路径（M不通过/步骤回退/禁荐股拦截/缺口率除零/防守层不足覆盖底线） | 1.5h |
| T5 | 修复 + 输出一份完整示例方案文档 | 1h |

**总计：12–14h，建议分 3 天完成**

### Phase 2（Web App）— 预计工时

| 任务 | 内容 | 估计 |
|---|---|---|
| T1 | 基础框架：步骤导航 + state 结构 + 回退规则 | 2h |
| T2 | 步骤 0–3 表单 + 双公式计算 + 2.6 七桶目标推算与合计校验 | 4.5h |
| T3 | 步骤 4 持仓归类 + 缺口计算（除零保护）+ 结构病检测 | 3h |
| T4 | 步骤 5–6 配置表单（含阶梯预填、层内比例覆盖） | 2h |
| T5 | 步骤 7 压测计算 + M 校验拦截 | 2h |
| T6 | 步骤 8 瀑布决策树可视化 + 自动生成执行表 | 3h |
| T7 | 步骤 9 + 导出MD/JSON/PDF | 2h |
| T8 | 整体 UI 打磨 + 边界测试 | 2h |

**总计：20.5h，建议分 1.5 周完成**

### 建议执行顺序

```
Week 1 前半：Skill T1（写 SKILL.md，这是核心工作量）
Week 1 后半：Skill T2-T5（测试 + 示例方案）
Week 2 前半：App T1-T4（基础框架 + 步骤0-6）
Week 2 后半：App T5-T8（步骤7-9 + 测试 + 导出）
```

---

## 六、验收标准

### 6.1 Skill 正常路径

- [ ] 完整走一遍 10 步，不跳步，输出完整方案 `.md` 文件
- [ ] 步骤 3：两公式取 min，展示计算过程，数字可复现
- [ ] 步骤 3：按 2.6 拆出 7 桶目标金额，合计 = 策略盘（±1元）
- [ ] 步骤 4：持仓映射到 7 个细粒度桶，缺口率排序正确（目标=0 桶置底显示 "—"）
- [ ] 步骤 6：B3 阶梯默认参数 1:1.5:2:2.6 预填展示
- [ ] 步骤 7：M 不通过时强制提示返回步骤 3，以步骤 7 为准
- [ ] 步骤 8：每月瀑布按决策树自动生成
- [ ] 汇总输出：结构与 SOP 一致，包含截至日期/来源/置信度
- [ ] 生存层与 B1 现金底线分别列示，无重复计提（见 2.7）

### 6.2 Skill 约束路径

- [ ] 步骤 5/8 输出中无任何股票/基金具体名称或代码
- [ ] 下单/调仓类请求被识别并拒绝执行，说明触发点
- [ ] 步骤 7 免责声明紧跟压测结果展示

### 6.3 Skill 回退路径

- [ ] 从步骤 5 回退到步骤 3 修改 M → 提示清除步骤 4-9（含 7 桶目标），确认后清除
- [ ] 从步骤 8 回退到步骤 6 修改阈值 → 步骤 7 标"参数已变更"，步骤 9 清除

### 6.4 Skill 边界测试

- [ ] M = 0 时：提示 M 必须 > 0，阻止进入步骤 7
- [ ] 策略盘 = 0（生存层 = 全部资产）时：提示无可投资金额，停在步骤 2
- [ ] 步骤 1 汇率为空时：提示填写汇率，阻止进入步骤 2
- [ ] 步骤 4 所有持仓归入 B1（全是现金）时：结构病检测触发"进取桶极度不足"
- [ ] **某桶目标 = 0 时**：缺口率显示 "—" 不报错，不参与排序；若该桶现有 > 0 触发"超配"
- [ ] **防守层金额 < B1 现金底线时**：提示防守层比例不足以覆盖底线，回步骤 3（见 2.6）
- [ ] **B1 底线用"月数"口径时**：Skill 提示与生存层重叠风险（见 2.7）

### 6.5 Web App 功能

- [ ] 步骤导航：已完成步骤可跳转，锁定步骤不可跳入
- [ ] 步骤 1 加总差额实时展示，差额 = 0 才可进入步骤 2
- [ ] 步骤 3 双公式实时计算，选择跌幅假设后立即更新结果；7 桶目标合计实时校验
- [ ] 步骤 7 M 不通过时"下一步"禁用，显示具体差额
- [ ] 回退时 Dialog 确认，确认后正确清除对应步骤数据
- [ ] 导出 JSON 包含 schema_version=1.2，导入旧版本（1.1）显示字段缺失警告
- [ ] 导出 MD 内容与页面数据一致，包含截至日期
- [ ] 免责声明在步骤 0 和最终导出页均出现

---

## 附录 A：v1.0 → v1.1 修复记录（存档）

| # | 问题 | 修复内容 |
|---|---|---|
| 1 | 步骤3公式硬编码偏激进 | 跌幅参数改为用户可选（默认55%，可选45%/50%/55%） |
| 2 | 步骤4桶粒度不足 | 从三账户改为7个细粒度桶 |
| 3 | 步骤7跌幅数据缺失 | 新增4情景×5资产类别参考跌幅表 |
| 4 | 步骤8瀑布逻辑未定义 | 新增完整决策树 |
| 5 | 步骤间回退状态规则缺失 | 新增回退处理规则 |
| 6 | 步骤6加仓阶梯默认参数缺失 | 补入 1:1.5:2:2.6 默认比例 |
| 7 | Skill文件结构与系统不兼容 | 改为SKILL.md单文件，模板内嵌 |
| 8 | 禁荐股约束无实现方案 | 新增prompt engineering规范 |
| 9 | 步骤9路径未适配用户vault | 明确适配obsidian目录结构 |
| 10 | 经验法阶段调整未量化 | 新增各阶段调整幅度定义 |
| 11 | JSON schema无版本字段 | 新增schema_version + 兼容说明 |
| 12 | 验收标准缺边界测试 | 补充边界条件测试用例 |
| 13 | 多方案管理未说明 | 新增多方案命名与管理规则 |
| 14 | Skill工时估算偏低 | T1调整为6-8h |

---

> 本文档不构成投资建议。所有投资决策由用户本人确认执行。
> PRD v1.2 · 2026-06-06
