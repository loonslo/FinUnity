# 资产配置规划 SOP — 产品需求文档（PRD v1.4）

> 基于：资产配置规划标准作业流程（SOP v2026-06-06）
> 修订：v1.4（2026-06-06）引入「方案类型 Profile」分流，解决境外敞口的通用性问题
> 目标：Skill 先行 → Web App 后做
> 计算深度：引导 + 模板（用户手动填数，不含实时行情）
> 不构成投资建议

---

## 变更说明（v1.3 → v1.4）

> 本版核心：原 7 桶把 **B6 境外权益**默认成"能直接买港股/美股"，但多数用户没有境外账号，只能通过 **QDII / 跨境 ETF / 对标境外指数的境内产品**获取境外敞口。原方案因此不具通用性。本版引入 **方案类型（Profile）**：框架（三层七桶 + 全部计算逻辑）保持不变，仅让 B6 的工具集与受影响步骤（1/5/6/7）随 Profile 切换。

| # | 问题 | 修复内容 |
|---|---|---|
| K | **境外敞口方案不通用**：B6 假设用户可直接购买境外权益，但无境外账号者只能买 QDII/跨境ETF/对标境外指数的境内产品，限购、溢价、额度规则完全不同 | 新增 **2.11 方案类型（Profile）**：分「有境外账号 / 无境外账号」两类；B6 实例、步骤 1/5/6/7 随 Profile 切换；新增 `meta.profile` 字段与步骤 0 选择动作 |

> v1.0→v1.3 修复记录见附录 A。

---

## 一、目标

把 10 步 SOP 做成两层工具：

| 层 | 形态 | 核心价值 |
|---|---|---|
| Phase 1 | Claude Skill | 对话式引导，逐步收集输入，生成个人化方案文档 |
| Phase 2 | Web App | 表单 + 自动计算 + 可视化，输出可打印报告 |

> **两期边界原则**：Phase 1 差异化价值是"对话式引导"，不是"自动计算"。计算与可视化是 Phase 2 价值。Phase 1 不做刚性工作流，而是"对话外壳 + 确定性脊椎"（见 2.8、3.0）。
>
> **通用性原则**：同一套三层七桶框架适配两类用户（有/无境外账号），通过 Profile 切换 B6 工具与相关约束，而非维护两份割裂方案（见 2.11）。

---

## 二、核心数据定义

> 本节集中定义全文复用的常量和规则，避免各步骤分散定义造成不一致。

### 2.1 细粒度桶定义（步骤 4、6、8 共用）

| 桶 ID | 名称 | 归属层/账户 | 说明 |
|---|---|---|---|
| B1 | 现金 | 防守层 | 活期/货基，可随时取用 |
| B2 | 弹药 | 防守层 | 专用于回撤加仓，非日常周转 |
| B3 | 核心宽基权益 | 稳健层 | 宽基指数，低跟踪误差，长期持有 |
| B4 | 价值/红利/低波权益 | 稳健层 | 防御型权益，关注估值区间 |
| B5 | 成长/科技权益 | 进取层 | 高波动，必须设比例上限 |
| B6 | 境外权益敞口 | 进取层 | 币种/市场分散工具；**具体实例随方案类型而定，见 2.11** |
| B7 | 训练仓/主题卫星 | 进取层 | 小比例，不向下摊平，写明退出条件 |

> **注**：用户可根据实际持仓增减子桶（如将 B3 拆分为 A股宽基/港股宽基），但默认框架为以上 7 桶。

> **层映射**：防守层 = {B1, B2}；稳健层 = {B3, B4}；进取层 = {B5, B6, B7}。

### 2.2 步骤 3 经验法调整系数（量化）

| 条件 | 调整幅度 |
|---|---|
| 基线 | 65% |
| 收入稳定且有持续结余 | +5% |
| 现育儿 | −5% |
| 计划 1 年内育儿 | −5% |
| 收入不稳定（自雇/合同工/待业） | −5% |
| 负债较重（月还款 > 收入 30%） | −5% |

> 取反推法和经验法结果中**更保守（更低）者**为最终权益比。
> 进取层占比 = 最终权益% 中分配给进取层的部分；防守层 = 100% − 稳健层% − 进取层%。三层落位见 2.6。

### 2.3 步骤 7 历史情景参考跌幅

> **免责**：以下为参考区间值，非精确回测数据，不含税费/汇率/择时，过去不代表未来。用户可自行修改。

| 情景 | B3 核心宽基 | B4 价值低波 | B5 成长科技 | B6 境外敞口 | B7 训练仓（默认=B5） | B1 现金/B2 弹药 |
|---|---|---|---|---|---|---|
| 2022 科技回调 | −30% | −15% | −55% | −35% | −55% | 0% |
| 2020 新冠闪崩 | −35% | −25% | −40% | −35% | −40% | 0% |
| 2008 系统性危机 | −55% | −40% | −60% | −50% | −60% | 0% |
| 2000 科技泡沫 | −45% | −20% | −80% | −45% | −80% | 0% |

> B7 训练仓默认跌幅 = 同情景 B5 值（已预填），用户可改为更深值。
> **Profile B（无境外账号）补充**：B6 用 QDII/跨境 ETF 表达时，场内品种可能出现**溢价回归**额外波动，最坏情景可在上表基础上再叠加用户自填的溢价回吐幅度（见 2.11）。

### 2.4 步骤 6 加仓阶梯默认参数

仅适用于 B3 核心宽基权益（低杠杆、低费率、可长期持有）：

| 回撤深度 | 资金来源 | 投入比例（相对 1 份） |
|---|---|---|
| −5% ～ −10% | 提高定投 | 1 |
| −10% ～ −20% | 定投 + 弹药 | 1.5 |
| −20% ～ −30% | 弹药为主 | 2 |
| > −30% | 弹药 + 备用通道 | 2.6 |

> 各档之和 = 弹药（B2）总额度。前提：B1 现金 ≥ 底线后才动手。

### 2.5 步骤 8 每月瀑布决策树

```
每月结余到账后，按以下顺序判断：

IF B1 现金 < B1 现金底线（步骤3，见2.7）:
  → 100% 补 B1，停一切权益新增
  → 结束

ELSE IF 步骤6中任意回撤信号已触发:
  → 按触发档位执行加仓阶梯（定投 + B2 弹药按比例投入 B3）
  → 结束

ELSE IF 防守层（B1+B2）< 防守层目标金额（2.6 推算）:
  → 100% 补 B2 弹药至目标
  → 结束

ELSE:
  → 计算各桶缺口率 = (目标 − 现有) / 目标   （目标=0 的桶按步骤4规则跳过）
  → 投入缺口率最大的桶
  → 若多桶缺口率相近（差值 < 5 个百分点），按 B3→B4→B5→B6 优先级排序
  → 注：B6 受 Profile 约束（换汇额度/QDII限购）时，若当期无法投入则顺延至下一可投桶（见2.11）

大额到账（理财到期等）：
  先确认 B1 ≥ 底线 → 再确认 B2 满 → 余额进"权益建设池"
  按 12–18 个月分批投入，权重向缺口率最大的桶倾斜
  高位（估值贵）则暂放货基按月慢投，不追高
```

### 2.6 三层比例 → 7 桶目标金额拆分规则

> 解决：步骤 3 只产出三层百分比，而步骤 4/6/8 需要 7 桶各自的目标金额。用户可在步骤 5 调整每桶上限时覆盖（冲突处理见 2.9）。

**第一步：三层金额**
```
防守层金额 = 防守% × 策略盘
稳健层金额 = 稳健% × 策略盘
进取层金额 = 进取% × 策略盘
```

**第二步：层内拆分到桶（默认比例，用户可调）**

| 层 | 桶 | 默认层内占比 | 约束 |
|---|---|---|---|
| 防守层 | B1 现金 | B1 目标 = B1 现金底线（2.7） | 不参与百分比分配，取绝对额 |
| 防守层 | B2 弹药 | = 防守层金额 − B1 目标 | < 0 时提示防守层不足覆盖底线，回步骤 3 |
| 稳健层 | B3 核心宽基 | 60% | 可调，B3+B4 = 100% |
| 稳健层 | B4 价值低波 | 40% | 同上 |
| 进取层 | B5 成长科技 | 50% | **必须设比例上限**（步骤 5），默认 ≤ 进取层 60% |
| 进取层 | B6 境外敞口 | 40% | 受 Profile 约束（见 2.11）；Profile B 因限购宜分批 |
| 进取层 | B7 训练仓 | 10% | 硬上限 = 策略盘 5%，超出回拨 B5/B6 |

**校验**：7 桶目标金额之和 == 策略盘（±1 元）。步骤 3 输出后即展示并校验。

> **Profile 影响默认值**：若用户在步骤 0 选「无境外账号且不使用 QDII/跨境工具」，则 B6 默认占比设 0，其 40% 按 2.9 回拨至 B5（进取层内）；其余 Profile 维持 50/40/10。

### 2.7 生存层与 B1 现金底线的关系（消除双算）

| 资金池 | 所在 | 口径 | 用途 | 计入策略盘 |
|---|---|---|---|---|
| **生存层** | 策略盘**之外** | 月刚性开支 × 保守月数（步骤 2） | 应急生存，不参与配置/加仓 | 否（已从总资产扣除） |
| **B1 现金底线** | 策略盘**之内** | 默认 = 策略盘 × 5% 或用户指定绝对额；**不用"月开支×月数"口径** | 配置周转 + 加仓前流动性缓冲 | 是 |

规则：两池物理与口径均独立、不重叠；加仓动用 B2 弹药，前提 B1 ≥ 底线；生存层任何时候不得动用；用户若坚持用"月数"定义 B1，Skill 须提示重叠风险。

### 2.8 Skill 稳定性架构（核心）

> **设计原则：对话只做外壳，脊椎做成确定性。** 凡涉及"记住数字、做算术、守规矩"的部分，一律不依赖模型自觉。三原则在单文件 SKILL.md 内即可实现。

**原则一 · 状态外置（消除漂移）**
- 维护 `plan_state.json`（结构 = 4.4 数据模型）；每步结束写入、每步开始先读取恢复上下文，而非依赖对话记忆。
- 跨步引用一律从 state 读取，禁止凭印象复述数字。
- 回退 = 删除 state 中对应步骤字段并标记 `dirty`，下游重算（3.8 落地）。

**原则二 · 计算外置（消除算术错误）**
- 步骤 1 折汇/对账、步骤 2 策略盘、步骤 3 双公式+min+七桶拆分+合计校验、步骤 4 缺口/缺口率（除零保护）/结构病、步骤 7 Σ(权重×跌幅)/M 校验——全部交内嵌脚本，模型只收集输入与解释结果。
- 副产物：该计算逻辑即 Phase 2 计算层，一次编写两期复用。

**原则三 · 校验节点化（消除"靠自觉"）**

| 校验项 | 实现方式（确定性，非模型自检） |
|---|---|
| 禁荐股 | 脚本/正则扫描步骤 5、8 输出，命中产品名/代码即拦截替换 |
| 不可跳步 | 进入步骤前读 state 判断前置字段是否齐全 |
| 七桶合计 | 步骤 3 后脚本校验 Σ桶目标 == 策略盘（±1元）|
| M 校验 | 步骤 7 脚本判断 worst_loss_abs ≤ M，否则强制回步骤 3 |
| 数据标注 | 输出模板内置"截至日期/来源/置信度"占位，缺失即拦截 |
| Profile 一致 | B6 实例须与 meta.profile 匹配（Profile A 不得出现 QDII 限购语义；Profile B 不得出现直接换汇/港美股语义）|

### 2.9 上限与目标冲突处理

cap_pct（×策略盘 = cap_amount）小于 2.6 桶目标时：
1. 该桶目标截断至 cap_amount。
2. 差额按同层内其他未触顶桶默认比例回拨；同层无可承接时，顺序 进取层→稳健层→防守层 B2。
3. 回拨后重校验"7 桶合计 == 策略盘"。
4. 所有权益桶均触顶 → 提示"上限过紧，剩余留存 B1/B2"并展示留存金额。

### 2.10 置信度判定标准

| 置信度 | 判定条件 |
|---|---|
| 高 | 用户直接提供且经对账无差额的确定数字 |
| 中 | 由用户输入经公式推算（攻防比、七桶目标、缺口率）|
| 低 | 含参考假设或历史区间（情景跌幅、长期期望年化、跌幅假设、溢价回吐）|

### 2.11 方案类型（Profile）：有 / 无境外账号【v1.4 新增 · 核心】

> 在步骤 0 由用户选择，写入 `meta.profile`，全程据此切换 B6 实例与相关约束。框架其余部分（三层七桶、计算逻辑、攻防比、加仓阶梯、瀑布决策树）两类完全相同。

| 维度 | **Profile A：有境外账号** | **Profile B：无境外账号** |
|---|---|---|
| B6 工具 | 直接持有境外权益（港股/美股/境外 ETF）| QDII 基金 / 跨境 ETF（场内）/ 对标境外指数的境内产品 |
| 计价币种 | 实际持有外币，步骤 1 需折汇 | 人民币计价（QDII 净值已含汇率），步骤 1 不重复折汇，仅披露隐含汇率风险 |
| 步骤 1 模板 | 含"海外券商 / 港卡"行，需填外币与汇率 | "海外券商 / 港卡"行可留空；境外敞口列在境内基金/ETF 项下 |
| 步骤 5 筛选标准 | 标的市场、做市/流动性、个人购汇额度（5万美元/年）| QDII 是否限购/暂停申购、场内**溢价率**（避免高溢价追高）、跟踪误差、QDII 额度、费率 |
| 步骤 6「境外额度」信号 | 个人购汇额度用尽 → 暂停换汇补 B6，改用存量外币/港币 | QDII 限购/暂停 或 场内溢价过高 → 暂停场内追高，转场外慢申购或暂存货基 |
| 步骤 7 压测 | 用 2.3 默认 B6 跌幅 | 在 2.3 基础上叠加用户自填**溢价回吐**幅度（场内品种）|
| 可投性 / 节奏 | 受换汇额度年度上限约束，分批换汇 | 受限购额度约束，宜小额分批、避开溢价高点 |
| B6 默认占比 | 进取层 40%（2.6）| 进取层 40%；若用户选「不使用 QDII/跨境工具」则 B6=0，按 2.9 回拨 B5 |

> **禁荐股仍适用**：两类 Profile 在步骤 5/8 均不得输出具体 QDII/ETF/股票名称或代码，只给类别与筛选标准（见 3.4）。

> **切换影响**：步骤 0 选定 Profile 后若中途变更，按 3.8 回退规则处理——视为影响 B6 语义，清除步骤 5-9 中与 B6 相关字段并提示重填。

---

## 三、Phase 1 — Claude Skill

### 3.0 Phase 1 与工作流的取舍说明

**工作流改造** = 把 10 步拆成有向图，每步独立节点、状态契约传值、控制流驱动。

**正面**：节点上下文短、遵循率高、长上下文衰减消失；状态成显式契约、漂移根除、回退=重算下游；计算/校验成代码节点、可单测、验收逐条变 eval；跳步/禁荐股变真控制流。

**代价**：削弱 Skill 对话差异化、与 Phase 2 边界模糊；Cowork Skill 无原生工作流引擎，只能用"状态文件+前置判断+脚本"模拟；成本上升、两期重复。

**决策（采纳）**：Phase 1 不做完整工作流，走中间路线——保留对话外壳，用 2.8 三原则把脊椎确定化；完整状态机留给 Phase 2。`plan_state.json` 结构与计算脚本 100% 顺延 Phase 2。

### 3.1 触发词

```
"开始资产配置"
"帮我做资产配置方案"
"走一遍资产配置 SOP"
```

### 3.2 整体流程

```
invoke skill → 读取/初始化 plan_state.json
  └─ 步骤 0：输出原则确认 + 选择方案类型 Profile（2.11）→ 用户确认 → 写 state
  └─ 步骤 1：收集全口径资产（按 Profile 调整模板，含平台申报总额对账）→ 脚本计算 → 写 state
  └─ 步骤 2：分三层 → 脚本算策略盘 → 写 state
  └─ 步骤 3：收集 M 和约束 → 脚本双公式取 min + 2.6 七桶拆分 + 合计校验 → 写 state
  └─ 步骤 4：持仓映射到7桶 → 脚本算缺口/缺口率/结构病 → 写 state
  └─ 步骤 5：资产类别偏好 + 上限（B6 按 Profile 给筛选标准，2.9 冲突重算）→ 无荐股 → 写 state
  └─ 步骤 6：生成信号→应对表（境外额度信号按 Profile 切换）→ 写 state
  └─ 步骤 7：压力测试（Profile B 叠加溢价回吐）→ 脚本 M 校验 → 写 state
  └─ 步骤 8：输出落地执行表（含每月瀑布决策树）→ 写 state
  └─ 步骤 9：输出复盘模板 + 版本说明
  └─ 汇总：由 state 渲染完整方案 .md 文件（标注 Profile）
```

### 3.3 每步 Skill 行为规范

**步骤 0（原则确认 + 方案类型）**
- 输出固定原则清单：只应对不预测 / 区分事实-推理-假设-观点 / 红线清单 / 不构成投资建议
- **选择方案类型 Profile**（2.11）：A 有境外账号 / B 无境外账号（含是否使用 QDII/跨境工具）；写入 `meta.profile`
- 用户输入 `确认` 后进入步骤 1；不可跳过

**步骤 1（全口径资产）**
- 账户分类模板按 Profile 调整：
  - Profile A：含"海外券商 / 港卡"行，填外币 + 汇率
  - Profile B："海外券商 / 港卡"行可留空，境外敞口归入境内基金/ETF 项
- 用户逐行填入：平台、项目名、币种、金额；并填每个平台的**申报总额**
- 脚本：外币 × 汇率 → 折本币（Profile B 多为 CNY，折汇平凡）
- **自检对账**：分平台逐项加总 == 该平台申报总额，展示每平台差额；≠0 须先平账
- 输出：

```
| 平台 | 项目 | 币种 | 原币金额 | 汇率 | 折本币 |
平台申报总额：____  逐项加总：____  差额：____
截至日期：____  数据来源：用户自填  方案类型：A/B
```

**步骤 2（三层分层）**
- 输入：月刚性开支、保守月数（3-6）、是否设深层储备（是/否，金额）
- 计算：生存层 = 月开支 × 月数；策略盘 = 总资产 − 生存层 − 深层储备
- 输出：三个数字 + 生存层明细；明确生存层为策略盘外独立池（2.7）

**步骤 3（攻防比）**
- 输入：年龄、家庭阶段、收入稳定性、负债、M、熊市跌幅假设（默认 55%，可选 45%/50%/55%）
- 脚本：反推法（初筛）权益上限 = M ÷ (策略盘 × 跌幅假设)；经验法 = 65% ± 阶段调整（2.2）；最终 = min
- 初筛定位说明：单一跌幅初筛，步骤 7 分桶精算复核，口径不同属正常；建议跌幅取偏保守值
- 输出：防守/稳健/进取 X/Y/Z%（各金额）+ 过程；B1 现金底线（默认策略盘×5%）；按 2.6 拆 7 桶目标，校验合计=策略盘
- Profile B 且不使用境外工具：B6 目标=0，回拨 B5（2.6）

**步骤 4（现状映射）**
- 逐笔持仓归类到 7 桶
- 脚本：每桶现有/目标/缺口/缺口率（目标>0→(目标−现有)/目标；目标=0且现有>0→"—"+触发"超配"；目标=0且现有=0→"—"）
- 结构病：超配/不足/碎片（同桶≥3只同风格）/重复暴露（人工判断）
- 输出：缺口表（缺口率排序，"—"置底）+ 结构病清单

**步骤 5（资产类别）**
- 7 桶 × 资产类别框架；用户选类别、填上限 cap_pct、禁买条件
- **B6 筛选标准按 Profile 给**（2.11）：
  - Profile A：标的市场、流动性、个人购汇额度
  - Profile B：是否限购/暂停、场内溢价率、跟踪误差、QDII 额度、费率
- 上限冲突按 2.9 重算并展示
- 禁荐股脚本拦截（3.4），两 Profile 均不得出现具体产品名/代码

**步骤 6（信号→应对）**
- 8 类预填信号；**「境外额度」信号按 Profile 切换**（2.11）
- B3 加仓阶梯预填默认参数（2.4），可改
- 输出：完整信号→动作表

**步骤 7（压力测试 — 精算复核）**
- 展示 2.3 跌幅表（免责紧跟）；用户确认/修改各情景跌幅（含 B7）
- **Profile B**：B6 可叠加用户自填溢价回吐幅度
- 脚本：组合峰谷 ≈ Σ(桶权重 × 跌幅)；各情景最坏浮亏 vs M
- 与步骤 3 关系：初筛 vs 精算复核，以步骤 7 为准
- 硬校验：浮亏 > M → 强制回步骤 3 调低权益%（或下调 B5/B7），重跑
- 输出：情景压测表 + 长期期望年化（可选，用户提供各桶年化假设时才算，置信度低）

**步骤 8（落地执行）**
- 自动填本周清单（步骤 2 生存层隔离操作）
- 展示 2.5 每月瀑布决策树（用户数据填充；B6 受 Profile 约束无法投入时顺延）
- 用户填：月结余、大额到账（可选）
- 自动生成每月投入分配（缺口率排序，"—"桶不参与）
- 输出：本周清单 / 每月瀑布 / 到账分配 三张表

**步骤 9（复盘模板）**
- 月/季/年复盘 checklist；版本管理说明（3.5）

**汇总输出**
- 由 `plan_state.json` 渲染完整 `.md`，**首部标注方案类型 Profile**
- 文件名：`资产配置方案-{A|B}-v1.0-YYYYMMDD.md`（多用户加 owner 前缀，见 3.5）
- 多方案用 `-v{N}` 区分

### 3.4 禁荐股约束实现规范

**Negative instruction**
```
禁止在步骤5、步骤8的输出中出现：
- 任何具体股票名称、代码（A股/港股/美股）
- 任何具体基金名称、基金代码（如 510300、QDII-xxx）
- 任何ETF、LOF、FOF、指数增强、QDII 的具体产品名
- 任何"推荐XX"、"可以买YY"等建议性表述
```

**允许的输出形式**
```
✅ "宽基权益类（追踪沪深300或全A），筛选：规模>50亿、跟踪误差<0.5%、费率<0.15%"
✅ "境外敞口类（Profile B：对标标普500/纳指的QDII或跨境ETF），筛选：非限购、场内溢价<2%、跟踪误差低"
✅ "候选产品由用户自行查找并在个案方案中二次确认"
❌ "可以考虑易方达沪深300ETF" / "推荐510300"
```

**验证步骤（确定性）**：步骤 5、8 输出前脚本扫描（6 位基金代码 / 股票代码 / 产品名词典）命中即拦截替换——不依赖模型自检（2.8 原则三）。

### 3.5 版本管理规范（适配用户 vault）

```
默认（单用户）：
  当前版本：wiki/topics/资产配置方案-{A|B}-v{N}-YYYYMMDD.md  （标记 🔄）
  历史版本：archive/资产配置方案-{A|B}-v{N-1}-YYYYMMDD.md     （标记 📦）
  状态文件：plan_state.json（与当前版本同目录）
  index 登记：wiki/index.md

多用户：以 owner 子目录隔离：
  wiki/topics/{owner}/资产配置方案-{A|B}-v{N}-YYYYMMDD.md
  （owner 取 meta.owner；为空退回默认）

文件内必须含：方案类型 Profile / 版本号 / 相对上一版 diff / 生成日期
多方案：大版本 = 攻防比调整；小版本 = 阈值/产品微调；Profile 变更视为大版本
```

### 3.6 Skill 文件结构

```
skills/asset-allocation/
├── SKILL.md       # 触发词 + Profile 选择 + 10步引导 + 模板 + 红线 + 调用脚本说明
└── calc.py        # 计算外置 + 校验节点（步骤1-7 全部计算，含 Profile 分支）
                   # （若仅允许单文件，则脚本以代码块内嵌于 SKILL.md，运行时落盘执行）
```

SKILL.md 内部分节：触发词 / 稳定性约定（读写 state + 调用 calc + 校验节点）/ 方案类型 Profile（2.11）/ 步骤引导 0-9 / 模板库 T1-T7 / 约束规则（红线 + 禁荐股 + 不可跳步 + Profile 一致）。

### 3.7 Skill 约束

| 约束 | 规则 |
|---|---|
| 红线 | 下单/调仓/连账户/改持仓记录 → 不执行，说明触发点后停止 |
| 产品建议 | 步骤5、8 不得输出具体股票/基金/QDII 代码（脚本拦截，3.4）|
| 数据标注 | 每个财务结论标注截至日期 + 来源 + 置信度（2.10）|
| 回测声明 | 步骤7 免责声明紧跟压测结果 |
| 不可跳步 | 读 state 校验前置：无 M 不得进步骤7；步骤1 未完成不得进步骤4；步骤3 未产出七桶不得进步骤4 |
| 状态一致 | 跨步引用一律从 state 读取（2.8 原则一）|
| Profile 一致 | B6 语义/筛选标准/信号须与 meta.profile 匹配（2.8 原则三、2.11）|
| 步骤回退 | 见3.8 |

### 3.8 步骤回退处理规则

> **落地**：回退 = 删除 state 中对应字段并标记下游 `dirty`，确认后脚本清除并重算。

| 回退场景 | 处理方式 |
|---|---|
| 步骤4-9 → 步骤1（改资产金额） | 步骤2-9 清除，从步骤2重算 |
| 步骤4-9 → 步骤2（改生存层） | 步骤3-9 清除 |
| 步骤4-9 → 步骤3（改 M/攻防比） | 步骤4-9 清除（含七桶目标）；提示"攻防比变更需重填" |
| 步骤7-9 → 步骤6（改信号阈值） | 仅步骤8-9 清除；步骤7 保留但标"参数已变更，建议重跑" |
| 步骤9 → 步骤8 | 仅步骤9 清除 |
| 任意步骤 → 步骤0（改 Profile） | 清除步骤5-9 中 B6 相关字段；提示"方案类型变更，B6 工具与信号需重选"（视为大版本）|

> 回退时 Skill 明确告知将清除哪些步骤，确认后执行。

---

## 四、Phase 2 — Web App

### 4.1 技术选型

- 单文件 HTML + React（Cowork 直接运行，无需部署）
- 跨会话持久化：JSON 导出/导入（不用 localStorage）
- 计算层：纯前端 JS，**移植 Phase 1 calc.py 逻辑（含 Profile 分支）**

### 4.2 页面结构

```
Header：当前步骤 / 总进度（步骤 X/10）+ 方案类型徽标（A/B）
Sidebar：步骤导航（已完成✓ / 当前→ / 待完成○ / 锁定🔒）
Main：当前步骤表单 + 说明
Footer：上一步 / 下一步（锁定未满足禁用）/ 保存草稿
```

### 4.3 各步骤 UI 规格

| 步骤 | UI 类型 | 关键计算 | 锁定条件 |
|---|---|---|---|
| 0 | 原则勾选框 + **Profile 单选（A/B + 是否用QDII）** | 无 | 全勾选 且 已选 Profile |
| 1 | 动态行表格（按Profile调模板）+ 汇率 + 平台申报总额 | SUM + 折汇 + 分平台对账差额 | 各平台差额 = 0 |
| 2 | 3 数字 input + 月数 slider | 自动算策略盘（生存层独立展示） | 策略盘 > 0 |
| 3 | 5 input/radio + M + 跌幅假设 | 双公式取 min，按2.6算7桶并校验合计 | M>0 且攻防比合理 且 七桶合计=策略盘 |
| 4 | 持仓归类下拉 + 自动缺口 | 缺口、缺口率（除零保护）、结构病 flag | 所有持仓已归类 |
| 5 | 7桶×资产类别卡片 + 上限 input（B6 文案按Profile） | 2.9 冲突重算，禁荐股约束 | 每桶至少选一类 |
| 6 | 8类信号预填表（境外额度按Profile）+ 阈值 | 无计算 | 所有阈值已填 |
| 7 | 情景跌幅可编辑表（ProfileB可加溢价回吐）+ 自动回撤 | Σ(桶权重×跌幅)，对比M | M校验通过 |
| 8 | 自动生成3张表（瀑布可视化）+ 可编辑 | 依赖前序数据 | 月结余已填 |
| 9 | 复盘checklist + 导出 | 无 | 无 |

### 4.4 数据模型（前端 State / plan_state.json）

> Phase 1 与 Phase 2 同构。

```javascript
{
  schema_version: "1.4",
  meta: {
    version: "v1.0", date: "YYYY-MM-DD", currency_base: "CNY",
    owner: "", plan_name: "",
    profile: "A",                 // v1.4 新增："A"(有境外账号) | "B"(无境外账号)
    use_overseas_tool: true       // v1.4 新增：Profile B 是否使用 QDII/跨境工具；false → B6=0
  },
  step0: { confirmed: false },
  step1: {
    accounts: [{ platform, item, currency, amount, rate, local_amount }],
    platform_declared: {}, platform_reconcile: {},
    total_local: 0, as_of_date: ""
  },
  step2: { monthly_expense:0, survival_months:6, deep_reserve:0, survival_total:0, strategy_total:0 },
  step3: {
    age:null, family_stage:"", income_stable:null, heavy_debt:null,
    M:0, bear_drop_assumption:0.55,
    equity_pct_inferred:0, equity_pct_experience:0, equity_pct_final:0,
    layer_pct:{ defense:0, steady:0, growth:0 },
    cash_floor:0,
    split:{ steady:{B3:0.6,B4:0.4}, growth:{B5:0.5,B6:0.4,B7:0.1} },  // use_overseas_tool=false 时 B6→0、B5→0.9
    buckets_target:{B1:0,B2:0,B3:0,B4:0,B5:0,B6:0,B7:0},
    buckets_target_sum_check:0
  },
  step4: {
    positions:[{item,bucket_id,current_amount}],
    buckets_current:{B1:0,B2:0,B3:0,B4:0,B5:0,B6:0,B7:0},
    buckets_gap:{B1:0,B2:0,B3:0,B4:0,B5:0,B6:0,B7:0},
    buckets_gap_rate:{B1:0,B2:0,B3:0,B4:0,B5:0,B6:0,B7:0},  // 目标=0→null（UI"—"）
    structure_issues:[]
  },
  step5: {
    buckets:[{ bucket_id, asset_class, criteria, cap_pct, forbidden:[] }],
    target_after_cap:{B1:0,B2:0,B3:0,B4:0,B5:0,B6:0,B7:0}
  },
  step6: {
    signals:[{type,trigger_condition,action}],   // 境外额度信号文案随 meta.profile
    ladder:{ tiers:[
      {drop_from:-0.05,drop_to:-0.10,source:"定投",multiplier:1},
      {drop_from:-0.10,drop_to:-0.20,source:"定投+弹药",multiplier:1.5},
      {drop_from:-0.20,drop_to:-0.30,source:"弹药为主",multiplier:2},
      {drop_from:-0.30,drop_to:null,source:"弹药+备用通道",multiplier:2.6}
    ]}
  },
  step7: {
    scenarios:[
      {name:"2022科技回调",drops:{B3:-0.30,B4:-0.15,B5:-0.55,B6:-0.35,B7:-0.55,B2:0,B1:0}},
      {name:"2020新冠闪崩",drops:{B3:-0.35,B4:-0.25,B5:-0.40,B6:-0.35,B7:-0.40,B2:0,B1:0}},
      {name:"2008系统性",  drops:{B3:-0.55,B4:-0.40,B5:-0.60,B6:-0.50,B7:-0.60,B2:0,B1:0}},
      {name:"2000科技泡沫",drops:{B3:-0.45,B4:-0.20,B5:-0.80,B6:-0.45,B7:-0.80,B2:0,B1:0}}
    ],
    premium_giveback_b6: 0,         // v1.4：Profile B 场内溢价回吐叠加幅度（可选）
    expected_annual_assumptions: null,
    results:[{scenario_name,loss_pct,loss_abs}],
    worst_loss_abs:0, m_check_pass:false
  },
  step8: { monthly_surplus:0, lump_sum_incoming:0, this_week:[], monthly_cascade:[], lump_sum_plan:[] },
  step9: { version_notes:"", review_checklist:{} }
}
```

**版本兼容**：导入草稿检查 `schema_version`；旧版（≤1.3）缺 `meta.profile / use_overseas_tool / premium_giveback_b6`，导入后提示在步骤 0/7 补齐（默认按 Profile A 处理）。

### 4.5 输出

- 导出 Markdown（含 Profile 标注，路径见 3.5）
- 导出 PDF（`@media print` 样式）
- 保存草稿 JSON（含 schema_version），可导入恢复

### 4.6 红线 & 边界

同 3.7，额外：
- 步骤 0 未选 Profile → 锁定
- 步骤 1 任一平台对账差额 ≠ 0 → 禁用下一步
- 步骤 3 七桶合计 ≠ 策略盘 → 禁用 + 显示差额
- 步骤 5 上限冲突 → 展示 2.9 重算；全桶触顶提示留存金额
- 步骤 7 M 不通过 → 禁用 + 红色提示差额
- 结果页底部固定免责声明；回退遵守 3.8（清除前弹确认 Dialog）

---

## 五、实施路径

### Phase 1（Skill）— 预计工时

| 任务 | 内容 | 估计 |
|---|---|---|
| T0 | 稳定性脚手架：plan_state.json + calc.py（含 Profile 分支）+ 读写约定 | 3.5h |
| T1 | 写 SKILL.md（10步引导 + 模板 + Profile 切换 + 2.6-2.11 规则）| 8–10h |
| T2 | 禁荐股脚本拦截（正则 + 产品名词典 + QDII，含测试用例）| 1.5h |
| T3 | 走完整流程测试（A/B 两 Profile 各一遍，校验合计、对账、B6 语义）| 2h |
| T4 | 边界与回退测试（M不通过/回退清状态/除零/上限冲突/Profile切换清B6）| 2h |
| T5 | 修复 + 输出 A、B 两份示例方案文档 | 1.5h |

**总计：18.5–21h，建议分 4 天完成**

### Phase 2（Web App）— 预计工时

| 任务 | 内容 | 估计 |
|---|---|---|
| T1 | 基础框架：步骤导航 + state（复用Phase1）+ Profile 徽标 + 回退 | 2.5h |
| T2 | 步骤 0–3 表单（Profile选择 + 模板切换）+ 双公式 + 七桶推算校验 | 4.5h |
| T3 | 步骤 4 持仓归类 + 缺口（除零保护）+ 结构病 | 3h |
| T4 | 步骤 5–6 配置（B6按Profile文案、2.9冲突重算、阶梯预填）| 2.5h |
| T5 | 步骤 7 压测（ProfileB溢价回吐）+ M 校验拦截 | 2.5h |
| T6 | 步骤 8 瀑布可视化 + 自动生成执行表 | 3h |
| T7 | 步骤 9 + 导出 MD/JSON/PDF | 2h |
| T8 | 整体 UI 打磨 + 边界测试（含 A/B 切换）| 2h |

**总计：22h，建议分 1.5 周完成**

### 建议执行顺序

```
Week 1 前半：Skill T0-T1（脚手架 + SKILL.md）
Week 1 后半：Skill T2-T5（拦截 + A/B 测试 + 两份示例）
Week 2 前半：App T1-T4（框架 + 步骤0-6，复用计算层）
Week 2 后半：App T5-T8（步骤7-9 + 测试 + 导出）
```

---

## 六、验收标准

### 6.1 Skill 正常路径

- [ ] A、B 两 Profile 各完整走一遍 10 步，由 state 渲染完整 `.md`
- [ ] 步骤 0：Profile 已选并写入 meta.profile
- [ ] 步骤 1：模板随 Profile 调整；分平台对账差额 = 0 才放行
- [ ] 步骤 3：双公式取 min 可复现；七桶合计 = 策略盘（±1元）；Profile B 不用工具时 B6=0 回拨 B5
- [ ] 步骤 4：缺口率排序正确，目标=0 桶显示 "—" 置底
- [ ] 步骤 5：B6 筛选标准按 Profile 给；上限<目标时按 2.9 重算
- [ ] 步骤 6：境外额度信号文案随 Profile 切换
- [ ] 步骤 7：M 不通过强制回步骤 3；Profile B 可叠加溢价回吐
- [ ] 汇总：首部标注 Profile，含截至日期/来源/置信度

### 6.2 Skill 稳定性路径

- [ ] 关闭对话重开，从 plan_state.json 恢复，数字一致
- [ ] 数值由脚本计算（抽查 3 处与手算一致）
- [ ] 跨步引用取自 state
- [ ] 步骤 5/8 无任何股票/基金/QDII 名称或代码（拦截可演示）
- [ ] 缺 M 进步骤 7 被拒并说明触发点
- [ ] **Profile 一致**：Profile A 输出不含 QDII 限购语义；Profile B 不含直接换汇/港美股语义

### 6.3 Skill 约束 & 回退路径

- [ ] 下单/调仓请求被拒并说明触发点
- [ ] 步骤 7 免责声明紧跟压测结果
- [ ] 步骤 5 回退步骤 3 改 M → 清除步骤 4-9（含七桶目标）后重算
- [ ] 步骤 8 回退步骤 6 → 步骤 7 标"参数已变更"，步骤 9 清除
- [ ] **改 Profile** → 清除步骤 5-9 中 B6 相关字段并提示重选

### 6.4 Skill 边界测试

- [ ] M = 0：提示必须 >0，阻止进步骤 7
- [ ] 策略盘 = 0：提示无可投资金额，停步骤 2
- [ ] 步骤 1 汇率为空（Profile A）：提示填写，阻止进步骤 2
- [ ] 步骤 4 全归 B1：结构病触发"进取桶极度不足"
- [ ] 某桶目标 = 0：缺口率 "—" 不报错；现有>0 触发"超配"
- [ ] 防守层 < B1 现金底线：提示回步骤 3
- [ ] 步骤 5 全桶触顶：提示留存金额
- [ ] **Profile B + 不使用境外工具**：B6=0，进取层只剩 B5/B7，校验合计仍成立

### 6.5 Web App 功能

- [ ] 步骤 0 未选 Profile → 锁定；选定后 Header 显示徽标
- [ ] 步骤 1 模板随 Profile 切换；分平台对账差额实时展示，=0 才进步骤 2
- [ ] 步骤 3 双公式实时计算；七桶合计实时校验
- [ ] 步骤 5 上限冲突实时按 2.9 重算；B6 文案随 Profile
- [ ] 步骤 7 M 不通过禁用下一步并显示差额
- [ ] 回退 Dialog 确认后正确清除数据；改 Profile 清 B6 相关
- [ ] 导出 JSON 含 schema_version=1.4 与 meta.profile，导入旧版显示字段缺失警告
- [ ] 导出 MD 含 Profile 标注与截至日期
- [ ] 免责声明在步骤 0 和最终导出页均出现

---

## 附录 A：历史修复记录（存档）

### A.1 v1.0 → v1.1（14 项）
跌幅参数可选 / 7桶细化 / 情景跌幅表 / 瀑布决策树 / 回退规则 / 加仓阶梯参数 / SKILL.md单文件 / 禁荐股规范 / vault路径 / 经验法量化 / schema_version / 边界测试 / 多方案管理 / 工时调整。

### A.2 v1.1 → v1.2（6 项）
| # | 问题 | 修复 |
|---|---|---|
| A | 三层→7桶拆分缺失 | 新增 2.6 |
| B | 生存层与B1双算 | 新增 2.7 |
| C | 步骤3与7口径不一致 | 初筛-复核定位 |
| D | 缺口率除零 | 除零保护 |
| E | B7硬编码矛盾 | 默认预填=B5 |
| F | 多用户路径混用 | owner子目录 |

### A.3 v1.2 → v1.3（4 项）
| # | 问题 | 修复 |
|---|---|---|
| G | Skill状态/计算/校验机制缺失 | 新增 2.8 + 3.0 |
| H | 步骤1对账对象不存在 | 新增平台申报总额 |
| I | 上限与目标冲突未定义 | 新增 2.9 |
| J | 长期年化/置信度悬空 | 年化改可选 + 新增 2.10 |

### A.4 v1.3 → v1.4（1 项）
| # | 问题 | 修复 |
|---|---|---|
| K | 境外敞口方案不通用 | 新增 2.11 方案类型 Profile（有/无境外账号），B6 与步骤1/5/6/7 随之切换 |

---

> 本文档不构成投资建议。所有投资决策由用户本人确认执行。
> PRD v1.4 · 2026-06-06
